spring:
  application:
    name: erp-chrome
  datasource:
    url: jdbc:postgresql://192.168.31.86:5432/erp_chrome_prod?useSSL=false&serverTimezone=GMT%2B8
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: admin@viji
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
server:
  port: 7040



xxl:
  job:
    admin:
      #调度中心部署根地址 [选填]：如调度中心集群部署存在多个地址则用逗号分隔。执行器将会使用该地址进行"执行器心跳注册"和"任务结果回调"；为空则关闭自动注册；
      addresses: http://192.168.31.86:7080/job-admin
    #行器通讯TOKEN [选填]：非空时启用；
    accessToken: 7A3224E0B086038B
    executor:
      # 执行器AppName [选填]：执行器心跳注册分组依据；为空则关闭自动注册
      appname: xxl-job-executor-chrome
      # 执行器注册 [选填]：优先使用该配置作为注册地址，为空时使用内嵌服务 ”IP:PORT“ 作为注册地址。从而更灵活的支持容器类型执行器动态IP和动态映射端口问题。
      address:
      # 执行器IP [选填]：默认为空表示自动获取IP，多网卡时可手动设置指定IP，该IP不会绑定Host仅作为通讯实用；地址信息用于 "执行器注册" 和 "调度中心请求并触发任务"；
      ip:
      # 执行器端口号 [选填]：小于等于0则自动获取；默认端口为9999，单机部署多个执行器时，注意要配置不同执行器端口；
      port: 9999
      # 执行器运行日志文件存储磁盘路径 [选填] ：需要对该路径拥有读写权限；为空则使用默认路径；
      logpath: /data/log/xxl-job/jobhandler
      # 执行器日志文件保存天数 [选填] ： 过期日志自动清理, 限制值大于等于3时生效; 否则, 如-1, 关闭自动清理功能；
      logretentiondays: 30


gyy:
  fieldsText: 店铺名称,发货单号,平台编号,发货时间,会员名称,商品类别,品牌,商品代码,商品名称,商品简称,规格代码,规格名称,数量,商品单位,成本单价,成本金额,实际单价,实际金额,仓库名称,订单标记,预计发货时间,让利金额,让利后金额,物流费用,商品税率,商品条码,物流公司,订单制单人,家装服务内容,支付时间,发票抬头,订单类型,供应商,赠品,审核人,其他服务费,物流单号,配货时间,分销商名称,币别,供应商货号,商品备注,批次,波次号,货主名称,包材,数据同步状态,母运单号,主播信息
  fieldsName: shopName,deliveryCode,platformCode,deliveryDateStr,vipName,categoryName,itemBrandName,itemCode,itemName,itemSname,itemSkuCode,itemSkuName,qtyStr,itemUnit,costPriceStr,costPriceAmountStr,priceStr,amountStr,warehouseName,tagName,planDeliveryDateStr,discountFeeStr,amountAfterStr,postFeeStr,taxRateStr,barcode,expressName,orderCreateName,jzContent,payTimeStr,invoiceTitle,orderTypeName,supplierName,isGift,approveName,otherServiceFeeStr,mailNo,distributionDateStr,drpName,currencyName,supplierOuterId,note,batchInfo,waveCode,shipperName,packageMaterialStr,syncStatusStr,parentMailNo,anchorInfo


