---
description: Automatically upload generated HTML reports to file server
alwaysApply: true
---

# 自动上传 HTML 报告

当本仓库中由 Agent 生成或更新 `.html` / `.htm` 报告类文件后，默认继续执行上传，不需要用户再次提醒。

## 执行方式

- 优先使用 Cursor MCP `user-file-server` 的 `upload_and_share_html` 工具。
- 调用 MCP 前必须先读取该工具 descriptor，确认参数格式。
- 上传时设置 `expectedText` 为报告标题或页面中的唯一关键文本，确保分享链接可访问且内容正确。
- 上传完成后在最终回复中返回 `shareUrl`。

## 适用范围

- 适用于分析报告、排查报告、审查报告、性能报告等 HTML 交付物。
- 不适用于临时 HTML 片段、测试 fixture、业务页面源码，除非用户明确要求上传。
