---
description: 调用测试环境 ERP HTTP API 时，通过固定测试账号登录获取 token
alwaysApply: true
---

# 测试 API 鉴权

调用**测试环境** ERP HTTP API（接口验证、排查补数、curl/Postman 示例等）且需要登录 token 时，**优先用本规则账号密码登录获取**，不要向用户反复索要 token。

测试业务 API **必须走域名网关**（见 `prod-api-domain-access.mdc`），禁止 IP:端口直连微服务。

## 网关基址（测试）

```text
https://erptest.ulanzi.cn:7000/api
```

完整 URL 形如：

```text
https://erptest.ulanzi.cn:7000/api/{service}/{相对路径}
```

`{service}` 示例：`auth`、`oms`、`wms`、`dmp`、`sys`、`tms`、`workflow` 等。

## 测试账号（默认）

| 项 | 值 |
|---|---|
| 账号 | `13250139159` |
| 密码 | `lrp13250139159` |
| userType | `erp`（与前端登录页一致） |

可选环境变量覆盖（进程 + Windows User 级，优先于上表）：

- `ERP_TEST_ACCOUNT`
- `ERP_TEST_PASSWORD`
- `ERP_TEST_GATEWAY_URL`（默认 `https://erptest.ulanzi.cn:7000/api`）

## 获取 token

登录接口（经网关）：

```text
POST /auth/user/accountLogin
Content-Type: application/json
```

请求体：

```json
{
  "account": "13250139159",
  "password": "lrp13250139159",
  "userType": "erp"
}
```

成功时 `data.accessToken` 即为 token；后续业务请求 Header：

- `Authorization: {accessToken}`（**不要**加 `Bearer ` 前缀；与前端 `sessionStorage.token` 一致）
- POST JSON 时加 `Content-Type: application/json`

## PowerShell 示例

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
$base = if ($env:ERP_TEST_GATEWAY_URL) { $env:ERP_TEST_GATEWAY_URL } else { [Environment]::GetEnvironmentVariable('ERP_TEST_GATEWAY_URL','User') }
if (-not $base) { $base = 'https://erptest.ulanzi.cn:7000/api' }
$account = if ($env:ERP_TEST_ACCOUNT) { $env:ERP_TEST_ACCOUNT } else { [Environment]::GetEnvironmentVariable('ERP_TEST_ACCOUNT','User') }
$password = if ($env:ERP_TEST_PASSWORD) { $env:ERP_TEST_PASSWORD } else { [Environment]::GetEnvironmentVariable('ERP_TEST_PASSWORD','User') }
if (-not $account) { $account = '13250139159' }
if (-not $password) { $password = 'lrp13250139159' }
$loginBody = @{ account = $account; password = $password; userType = 'erp' } | ConvertTo-Json
$login = Invoke-RestMethod -Uri "$base/auth/user/accountLogin" -Method Post -ContentType 'application/json' -Body $loginBody -TimeoutSec 20
if ($login.code -ne 200 -or -not $login.data.accessToken) { throw "登录失败: $($login.msg)" }
$token = $login.data.accessToken
$headers = @{ Authorization = $token; 'Content-Type' = 'application/json' }
# 示例：Invoke-RestMethod -Uri "$base/wms/warehouseLocationMapping/paging" -Method Post -Headers $headers -Body '{"currPage":1,"pageSize":10,"params":{}}'
```

## 使用原则

- 仅在用户要求或排查需要**主动调测试 API** 时使用；默认仍优先 MCP / 只读查库。
- token 仅用于**当前会话**的 HTTP 调用；**不要**写入 Git 提交、脚本文件、聊天记录中的持久化文档或最终回复全文。
- 登录失败（如账号锁定、密码变更）时告知用户，可请其确认密码或提供新凭据；可尝试读取 `ERP_TEST_*` 环境变量是否已覆盖默认值。
- 生产环境 API 鉴权**不适用**本规则；生产 token 仍由用户临时提供，见 `prod-api-domain-access.mdc`。
