---
description: 按环境切换并拉取最新代码后再排查问题
alwaysApply: true
---

# 排查问题时的代码分支

排查**线上/环境问题时**，必须先对齐**该环境实际部署的代码分支**，并拉取**最新远程代码**后再读代码、查库、看日志。不要基于本地 feature 分支或未同步的旧代码做结论。

## 分支对照（本仓库 `cloud-erp-backend`）

| 排查环境 | 使用分支 | 远程 |
|---|---|---|
| 测试（test） | `test` | `origin/test` |
| UAT | `uat` | `origin/uat` |
| 生产（prod） | `main` | `origin/main` |

用户未明确环境时，根据 Jira/工单描述、Nacos namespace、MCP 连接（如 `pg-test-*` / `pg-uat-*` / `pg-prod-*`）或用户口头说明推断；仍不确定则先问。

## 每次排查前执行

1. **确认工作区**：`git status`。若有未提交改动，先告知用户；未经确认不要 `stash` / 丢弃或强切分支。
2. **切到目标分支**：`git checkout <branch>`（如已在目标分支可跳过）。
3. **拉取最新**：`git pull origin <branch>`（或 `git fetch origin <branch>` 后 `git merge origin/<branch>`）。
4. **再开始排查**：读代码、查测试/UAT/生产库、Redis、Loki、RocketMQ 等。

PowerShell 示例（生产排查）：

```powershell
cd D:\cloud-erp-backend
git status
git checkout main
git pull origin main
```

## 注意

- 排查结论应注明依据的**分支名与 pull 后的 commit**（如 `git rev-parse --short HEAD`），便于与线上版本对照。
- 本规则是**只读排查**时的代码基线；修复 bug 的开发分支仍按团队 MR 流程，不在此规则内强制。
- 需要对照前端行为时，同步遵循 `frontend-reference-debugging` 中的前端分支约定。
