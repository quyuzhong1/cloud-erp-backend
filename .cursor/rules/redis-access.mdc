---
description: Redis connection reference for test and production debugging
alwaysApply: true
---

# Redis 连接参考

应用 Redis 配置来自 Nacos `common.yml`（前缀 `redis.*`）；WMS 另有 `db-wms.yml` 中的 `inventoryredis.*`、`virtualinventoryredis.*`。

**排查问题时，若现象与缓存、锁、会话、幂等、延迟队列、平台 token 等相关，应主动查 Redis**，不要只查库和日志。优先使用 Cursor MCP：`redis-test`（测试）、`redis-prod`（生产只读）。MCP 不可用时再用本机 `redis-cli`。

## 何时查 Redis

结合代码中的 `RedisCacheConstants`、`RedisUtil`、`RedissonClient`、`@Idempotent` / `@DistributeLocker` 使用点，以下场景应查 Redis：

- **登录/鉴权**：token 是否存在或过期（如 `auth:login:token:`）、登录错误次数锁
- **幂等/防重**：重复提交、MQ 重复消费是否已有幂等 key
- **分布式锁**：`RLock` 是否未释放导致业务卡住
- **平台授权/token**：三方店铺 token、OAuth state 缓存
- **业务缓存**：接口或任务把中间结果、限流计数、临时状态写入 Redis
- **延迟队列/消息**：Redisson `RDelayedQueue`、`RBlockingQueue` 是否有积压
- **WMS 库存**：`inventoryredis` / `virtualinventoryredis` 对应 db 8、9 的库存相关 key

查前先读代码确认 **key 前缀、db index、TTL**；生产默认只读，禁止删 key 或改值，除非用户明确要求修缓存。

## Cursor MCP

| MCP server | 环境 | 地址 | 密码 | 写操作 |
|------------|------|------|------|--------|
| `redis-test` | 测试 | `172.16.100.60:32600` | 无 | 允许 |
| `redis-prod` | 生产 | `172.16.100.10:30590` | 无 | 禁止（只读排查） |

- 类型：单机 Redis
- 调用 MCP 前先读取对应 server 的 tool schema
- 默认 MCP 连接 **database 0**；业务实际使用的 DB index 见下节，必要时用 `redis-cli -n <index>` 查指定库

## 业务 DB index（Nacos 配置，与 MCP 默认 db 0 可能不同）

| 用途 | 配置前缀 | 测试 db-index | 生产 db-index |
|------|----------|---------------|---------------|
| 通用 Redis | `redis` | 1 | 1（与 Nacos `common.yml` 一致，生产以 Nacos 为准） |
| WMS 库存 | `inventoryredis` | 8 | 8（WMS `db-wms.yml`） |
| WMS 虚拟库存 | `virtualinventoryredis` | 9 | 9（WMS `db-wms.yml`） |

## 本机 redis-cli 示例（Windows）

无密码、指定 database：

```powershell
redis-cli -h 172.16.100.10 -p 30590 -n 1
redis-cli -h 172.16.100.60 -p 32600 -n 1
```

常用只读命令：`GET`、`HGETALL`、`TTL`、`KEYS`（大库慎用）、`SCAN`。

## 生产环境注意

- 生产 Redis MCP 已关闭 INSERT/UPDATE/DELETE/CREATE/DROP，仅用于排查
- 不要在仓库、规则或回复中写入 Redis 密码（当前测试/生产均为无密码）
- 需要改生产缓存数据时，须用户明确要求并评估业务影响
