---
description: Jenkins 自动部署（仅 test/dev）；凭据从用户环境变量读取
alwaysApply: true
---

# Jenkins 部署参考

用户要求通过 Jenkins API **自动部署**时，使用本规则凭据与任务白名单。**仅允许 test、dev 环境**；禁止触发 uat/prod/归档 等任务。

## Jenkins 基址

```text
http://172.16.100.90:8080
```

内网实际节点可能解析为 `172.16.100.11:8080`；API 调用统一用上述基址。

## 凭据（默认）

| 项 | 值 |
|---|---|
| 用户名 | `developer` |
| API Token | 从 `JENKINS_TOKEN` 读取（**禁止**写入仓库、规则正文或聊天记录中的持久化文档） |

可选环境变量覆盖（进程 + Windows User 级，优先于上表）：

- `JENKINS_URL`（默认 `http://172.16.100.90:8080`）
- `JENKINS_USER`（默认 `developer`）
- `JENKINS_TOKEN`

## 允许部署的 Job（白名单）

仅可触发以下 Workflow Job（`NAMESPACE` 与 Job 对应）：

| 环境 | Job | `NAMESPACE` 参数 |
|---|---|---|
| 测试 | `test-all` | `test` |
| 测试（蓝绿） | `test-all-bluegreen` | `test` |
| 开发 | `dev-all` | `dev` |

## 禁止触发的 Job（黑名单）

以下 Job **一律不得**由 Agent 自动触发，即使用户口头要求也应拒绝并说明仅支持 test/dev：

- `prod-all`、`prod-all-bluegreen`、`prod-archive-all` 及一切 `prod-*`
- `uat-all`、`uat-archive-all` 及一切 `uat-*`
- 其他非 test/dev 的后端/前端发布任务（如 `prod-erp-web-*`、`uat-server-*` 等）

## 触发方式

1. 读取 `JENKINS_URL` / `JENKINS_USER` / `JENKINS_TOKEN`（进程环境 + Windows User 级）。
2. `GET /crumbIssuer/api/json` 取 CSRF crumb。
3. `POST /job/{jobName}/buildWithParameters`，Header：`Authorization: Basic base64(user:token)` + `Jenkins-Crumb`。

部署单个服务时，在 `test-all` / `dev-all` 上设置对应 `DEPLOY_*=true`，并将 `DEPLOY_CHANGED=false`（避免误发未变更服务）。

常用示例（仅部署 DMP，测试环境）：

- `NAMESPACE=test`
- `DEPLOY_CHANGED=false`
- `DEPLOY_DMP=true`
- 其余 `DEPLOY_*=false`

## PowerShell 示例

```powershell
$base = if ($env:JENKINS_URL) { $env:JENKINS_URL } else { [Environment]::GetEnvironmentVariable('JENKINS_URL','User') }
if (-not $base) { $base = 'http://172.16.100.90:8080' }
$user = if ($env:JENKINS_USER) { $env:JENKINS_USER } else { [Environment]::GetEnvironmentVariable('JENKINS_USER','User') }
$token = if ($env:JENKINS_TOKEN) { $env:JENKINS_TOKEN } else { [Environment]::GetEnvironmentVariable('JENKINS_TOKEN','User') }
if (-not $user) { $user = 'developer' }
if (-not $token) { throw 'MISSING_JENKINS_TOKEN' }

$jobName = 'test-all'  # 仅 test-all | test-all-bluegreen | dev-all
$pair = [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("${user}:${token}"))
$headers = @{ Authorization = "Basic $pair" }
$crumb = Invoke-RestMethod -Uri "$base/crumbIssuer/api/json" -Headers $headers -TimeoutSec 20
$headers[$crumb.crumbRequestField] = $crumb.crumb

$params = @{
  NAMESPACE = 'test'
  DEPLOY_CHANGED = 'false'
  DEPLOY_DMP = 'true'
  DEPLOY_ALL = 'false'
}
$body = ($params.GetEnumerator() | ForEach-Object { "$($_.Key)=$($_.Value)" }) -join '&'
Invoke-WebRequest -Uri "$base/job/$jobName/buildWithParameters" -Method Post -Headers $headers -Body $body -UseBasicParsing
```

## 使用原则

- 仅在用户要求部署、或排查需发布 test/dev 修复版本时主动触发。
- 触发后轮询 `/job/{jobName}/lastBuild/api/json` 确认 `result` 与 Git SHA，再验证目标服务版本（如 Nacos / 业务行为）。
- Token 仅用于当前会话与用户环境变量；**不要**提交到 Git、不要写入本规则文件。
- `JENKINS_TOKEN` 缺失时提示用户在 Windows 用户环境变量中配置后**完全重启 Cursor**。
