---
description: EasyExcel 导出 xlsx 模板须使用共享字符串，禁止 inlineStr 列表占位符
globs: erp-server/erp-server-file/src/main/resources/excel/**
alwaysApply: false
---

# EasyExcel 导出模板：共享字符串规范

修改 `erp-server-file` 下 `src/main/resources/excel/**` 中的 `.xlsx` 模板，或调整 `Export*Handler` / `getExcelPath()` 指向的模板时，**列表区与表头须使用共享字符串（`t="s"`）**，不得使用内联字符串（`t="inlineStr"`）存放 `{.fieldName}` 占位符。

## 原因

- 分页导出链路（`AbstractPageFileEventHandler`）历史上使用 `FillConfig.forceNewRow(false)`。
- 模板为 `inlineStr` 时，EasyExcel 填充只写 `<v>`、不清理 `<is><t>{.code}</t></is>`，Excel/WPS 仍显示占位符，表现为「导出文件没有数据」。
- 共享字符串格式（`t="s"` + `sharedStrings.xml`）与多数存量模板一致，在上述填充方式下可正常显示。

## 必须

- 列表占位符（如 `{.code}`、`{.shopName}`）放在 **`sharedStrings.xml`**，单元格为 `<c r="A2" t="s"><v>索引</v></c>`。
- 表头中文/英文同理：优先 **共享字符串**，不要整表用 `inlineStr`。
- 新增或改列后，占位符字段名与 DTO/导出 VO 属性一致（EasyExcel 填充规则不变）。
- 参考合格模板：`excel/scm/purchaseOrderExport.xlsx`、`excel/wms/qcBill.xlsx`（改前可用下方自检）。

## 禁止

- 禁止提交列表数据行带 `t="inlineStr"` 且内容为 `{.xxx}` 的模板。
- 禁止仅依赖 `forceNewRow(true)` 代码修复来「兼容」inlineStr 新模板（代码侧可兜底，**模板仍须共享字符串**）。

## 在 Excel 中编辑后

Excel 另存/粘贴后容易把单元格变成 `inlineStr`。提交前必须自检：

1. 解压 `sheet1.xml`：数据行（通常第 2 行）**不应**出现 `t="inlineStr"` + `{.字段}`。
2. 存在 `xl/sharedStrings.xml`，且 `{.code}` 等占位符在共享字符串表中。
3. 本地或测试环境导出一份，打开 xlsx 确认数据行显示真实值而非 `{.code}`。

若已为 `inlineStr`，在保留当前表头/列/占位符内容不变的前提下，转换为共享字符串再提交（勿用旧版模板回滚顶替新列）。

## 自检命令（可选）

```powershell
# 将 template.xlsx 复制为 template.zip 后解压，检查 A2
Select-String -Path xl\worksheets\sheet1.xml -Pattern 'inlineStr|A2'
Select-String -Path xl\sharedStrings.xml -Pattern '\{\.'
```

`inlineStr` 为 0、`.{` 出现在 `sharedStrings.xml` 即为合格。
