spring:
  application:
    name: erp-gateway
  profiles:
    active: uat
server:
  port: 9000

logging:
  config: classpath:logback-spring.xml
  level:
    com.alibaba.nacos.client.naming: WARN
    com.alibaba.nacos.client.config.impl: WARN
    io.seata: INFO
    com.zaxxer.hikari: INFO
    com.xxl.job.core.thread.ExecutorRegistryThread: INFO