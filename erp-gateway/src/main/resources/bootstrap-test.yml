spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.100.60:30848
        namespace: ${NACOS_NAMESPACE:test}
      config:
        server-addr: 172.16.100.60:30848
        namespace: test
        file-extension: yml
        shared-configs: gateway.yml,common.yml,db-gateway.yml
#    gateway:
#      routes:
#        - id: admin_route
#          uri: lb://erp-admin
#          predicates:
#            - Path=/api/sys/**
#          filters:
#            - RewritePath=/api/(?<segment>.*), /$\{segment}

