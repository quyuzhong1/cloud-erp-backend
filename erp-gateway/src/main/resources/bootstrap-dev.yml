spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.110.12
        namespace: dev
      config:
        server-addr: 172.16.110.12
        namespace: dev
        file-extension: yml
        shared-configs: gateway.yml,common.yml
#    gateway:
#      routes:
#        - id: admin_route
#          uri: lb://erp-admin
#          predicates:
#            - Path=/api/sys/**
#          filters:
#            - RewritePath=/api/(?<segment>.*), /$\{segment}

