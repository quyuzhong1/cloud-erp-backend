spring:
  cloud:
    nacos:
      discovery:
        server-addr: deploy-nacos-svc:8848
        namespace: ${NACOS_NAMESPACE:dev}
        ip: 172.16.100.60
        port: 30700
      config:
        server-addr: deploy-nacos-svc:8848
        namespace: dev
        file-extension: yml
        shared-configs: gateway.yml,common.yml,db-gateway.yml
#    gateway:
#      routes:
#        - id: admin_route
#          uri: lb://erp-admin
#          predicates:
#            - Path=/api/sys/**
#          filters:
#            - RewritePath=/api/(?<segment>.*), /$\{segment}

