<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.scm.mapper.SupplierMapper">
    <!-- 根据实体对象生成resultType -->
    <resultMap id="BaseResultMap" type="com.erp.model.scm.entity.SupplierEntity">
        <id column="id" property="id" />
        <result column="code" property="code" />
        <result column="voucher_no" property="voucherNo" />
        <result column="create_user_id" property="createUserId" />
        <result column="create_user_name" property="createUserName" />
        <result column="create_time" property="createTime" />
        <result column="name" property="name" />
        <result column="approve_status" property="approveStatus" />
        <result column="phase" property="phase" />
        <result column="grade_id" property="gradeId" />
        <result column="category_id" property="categoryId"/>
        <result column="disabled" property="disabled"/>
        <result column="srm_disabled" property="srmDisabled"/>
        <result column="pay_method_id" property="payMethodId"/>
        <result column="payment_condition" property="paymentCondition"/>
        <result column="purchase_user_id" property="purchaseUserId"/>
        <result column="purchase_user_name" property="purchaseUserName"/>
        <result column="approve_time" property="approveTime"/>
        <result column="identification_code" property="identificationCode"/>
        <result column="registered_capital" property="registeredCapital"/>
        <result column="property_json" property="propertyJson" typeHandler="com.erp.server.scm.handler.ObjectJsonArrayHandler"/>
        <result column="product_category_json" property="productCategoryJson" typeHandler="com.erp.server.scm.handler.ObjectJsonArrayHandler"/>
        <result column="application_category_json" property="applicationCategoryJson" typeHandler="com.erp.server.scm.handler.ObjectJsonArrayHandler"/>
        <result column="certificate_json" property="certificateJson" typeHandler="com.erp.server.scm.handler.ObjectJsonArrayHandler"/>
    </resultMap>



    <select id="paging" resultType="com.erp.model.scm.dto.SupplierDTO$PagingViewDTO">
        SELECT
        id,
        code,
        voucher_no as voucherNo,
        create_user_id AS createUserId,
        create_user_name AS createUserName,
        create_time AS createTime,
        name,
        approve_status AS approveStatus,
        phase,
        grade_id AS gradeId,
        category_id AS categoryId ,
        disabled AS disabled,
        srm_disabled AS srmDisabled,
        pay_method_id AS payMethodId,
        payment_condition as paymentCondition,
        purchase_user_id AS purchaseUserId,
        purchase_user_name AS purchaseUserName,
        approve_time as approveTime,
        identification_code as identificationCode,
        registered_capital as registeredCapital,
        property_json as propertyJson,
        product_category_json as productCategoryJson,
        application_category_json as applicationCategoryJson,
        certificate_json as certificateJson
        FROM supplier
        WHERE
        ${params.sqlMap.default}
         AND is_deleted=FALSE
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        create_time DESC
    </select>

    <select id="getExportSupplier" resultType="com.erp.model.scm.dto.SupplierDTO$PagingViewDTO">
     SELECT
        id,
        code,
        voucher_no as voucherNo,
        create_user_id AS createUserId,
        create_user_name AS createUserName,
        create_time AS createTime,
        name,
        approve_status AS approveStatus,
        phase,
        grade_id AS gradeId,
        category_id AS categoryId ,
        disabled AS disabled,
        srm_disabled AS srmDisabled,
        pay_method_id AS payMethodId,
        payment_condition as paymentCondition,
        purchase_user_id AS purchaseUserId,
        purchase_user_name AS purchaseUserName,
        approve_time as approveTime,
        identification_code as identificationCode,
        registered_capital as registeredCapital,
        property_json as propertyJson,
        product_category_json as productCategoryJson,
        application_category_json as applicationCategoryJson,
        certificate_json as certificateJson
        FROM supplier
        WHERE ${params.sqlMap.default} AND is_deleted=FALSE
        <!--权限-->
        <if test="params.permissionSql!=null and params.permissionSql!=''">
            ${params.permissionSql}
        </if>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        create_time DESC
    </select>
    <select id="listSupplierByCategoryType" resultType="com.erp.model.scm.dto.SupplierDTO$SupplierSimpleDTO">
        SELECT
          id,code,name,disabled,approve_status
        FROM  supplier WHERE is_deleted=FALSE
        <if test="approveStatus != null and approveStatus != '' ">
            and approve_status = #{approveStatus}
        </if>
        AND category_id IN
        (SELECT id FROM dict_basic WHERE is_deleted= FALSE AND type=#{type} AND value=#{value} )
        ORDER BY  create_time DESC
    </select>
    <select id="pagingSelect" resultType="com.common.business.dto.base.BaseDropDownDTO$RemarkDTO">
        select id,name as value,code,disabled,payment_condition
        from
        (
        select
        sup.id,
        sup.name,
        sup.code,
        sup.payment_condition,
        sup.disabled
        from supplier sup
        where
        sup.is_deleted = false
        <if test="params.searchKeyword != null and params.searchKeyword != ''">
            and ((sup.code = #{params.searchKeyword}) or (sup.id = #{params.searchKeyword})  or sup.name ilike concat('%', #{params.searchKeyword}::text ,'%'))
        </if>
        <if test="params.disabled != null">
            and sup.disabled=#{params.disabled}
        </if>
        <if test="params.approveStatusList != null and params.approveStatusList.size > 0">
            and sup.approve_status in
            <foreach collection="params.approveStatusList" item="item" open="(" close=")">
                #{item}
            </foreach>
        </if>
        order by
        <if test="params.searchKeyword != null and params.searchKeyword != ''">
            case when sup.name ILIKE concat(#{params.searchKeyword}::text,'%') THEN 1
            when sup.name ILIKE concat('%',#{params.searchKeyword}::text) THEN 2
            when sup.name ILIKE concat('%',#{params.searchKeyword}::text,'%') THEN 3
            end , sup.name,
        </if>
        sup.code asc,sup.disabled asc
        ) supplier
    </select>


</mapper>
