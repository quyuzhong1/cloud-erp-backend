spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.100.60:32570
        namespace: ${NACOS_NAMESPACE:test}
      config:
        server-addr: 172.16.100.60:32570
        namespace: test
        file-extension: yml
        shared-configs: common.yml,erp-msg.yml
        extension-configs:
          - data-id: erp-release-control.yml
            group: DEFAULT_GROUP
            refresh: true

  #显示日期的
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

