spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.100.12
        namespace: test
      config:
        server-addr: 172.16.100.12
        namespace: test
        shared-configs: common.yml

  #显示日期的
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

