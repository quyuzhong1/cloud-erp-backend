spring:
  cloud:
    nacos:
      discovery:
        server-addr: 10.107.36.15
        namespace: test
      config:
        server-addr: 10.107.36.15
        namespace: test
        file-extension: yml
        shared-configs: common.yml,erp-msg.yml

  #显示日期的
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

