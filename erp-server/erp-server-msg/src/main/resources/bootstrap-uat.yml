spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.100.10
        namespace: uat
      config:
        server-addr: 172.16.100.10
        namespace: uat
        file-extension: yml
        shared-configs: common.yml,erp-msg.yml
#显示日期的
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8









