<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.dmp.mapper.PlatformApiTaskMapper">

    <sql id="Base_Column_List">
        id,platform_id,api_id,
        api_code,api_name,interval_time,
        last_time,next_time,state,
        create_time
    </sql>

    <select id="selectApiTask" resultType="com.erp.model.dmp.dto.JobTaskDTO">
        SELECT
        pat.id,
        pat.platform_id,
        pat.api_id,
        pat.interval_time,
        pat.last_time,
        pat.next_time,
        pat.state,
        pat.create_time,
        pat.update_time,
        pat.retry_count,
        pa.api_code,
        pa.api_name,
        p.task_name,
        p.name AS platformName
        FROM platform_api_task pat
        LEFT JOIN platform_api pa ON pat.api_id = pa.id
        LEFT JOIN platform p ON pa.platform_id = p.id
        WHERE
        (pat.next_time &lt; #{localTime} and pat.next_time IS NOT NULL)
--         AND state=1
        and pat.is_deleted = false
        limit #{pageSize} offset #{pageNumber}
    </select>

    <insert id="batchInsert" parameterType="com.erp.model.dmp.entity.PlatformApiTaskEntity">
        insert into platform_api_task
        (platform_id, api_id, api_code, api_name, interval_time,
        last_time, next_time, state, create_time)
        values
        <foreach collection="taskEntityList" item="task" separator=",">
            (#{task.platformId}, #{task.apiId}, #{task.apiCode}, #{task.apiName}, #{task.intervalTime},
            #{task.lastTime}, #{task.nextTime}, #{task.state}, #{task.createTime})
        </foreach>
    </insert>

    <update id="updateTaskTypeState">
        UPDATE platform_api_task SET state = #{state} WHERE id IN
        <foreach item="task" collection="jobTaskDTOList" separator="," open="(" close=")" index="">
            #{task.id}
        </foreach>
    </update>
</mapper>
