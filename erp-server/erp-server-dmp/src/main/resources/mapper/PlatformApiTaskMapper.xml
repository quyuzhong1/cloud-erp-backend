<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.dmp.mapper.PlatformApiTaskMapper">

    <resultMap id="BaseResultMap" type="com.erp.model.dmp.entity.PlatformApiTaskEntity">
        <result column="api_param" jdbcType="OTHER" property="apiParam" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler" />
    </resultMap>

    <sql id="Base_Column_List">
        id,platform_id,api_id,
        api_code,api_name,interval_time,
        last_time,next_time,status,
        create_time
    </sql>

    <select id="selectApiTask" resultType="com.common.business.dto.JobTaskDTO">
        SELECT
        pat.id,
        pat.dict_platform,
        pat.interval_time,
        pat.last_time,
        pat.next_time,
        pat.status,
        pat.create_time,
        pat.update_time,
        pat.retry_times,
        pat.shop_name,
        pat.shop_id,
        pat.bill_type,
        pat.operate_type,
        pat.api_param,
        pat.platform_api_id,
        pat.api_code,
        pat.api_name,
        pat.platform_category
        FROM platform_api_task pat
        WHERE (pat.next_time  &lt; #{localTime} and pat.next_time IS NOT NULL)
        and pat.is_deleted = false and pat.disabled = FALSE
        and operate_type = #{operateType}
    </select>

    <insert id="batchInsert" parameterType="com.erp.model.dmp.entity.PlatformApiTaskEntity">
        insert into platform_api_task
        (platform_id, api_id, api_code, api_name, interval_time,
        last_time, next_time, status, create_time)
        values
        <foreach collection="taskEntityList" item="task" separator=",">
            (#{task.platformId}, #{task.apiId}, #{task.apiCode}, #{task.apiName}, #{task.intervalTime},
            #{task.lastTime}, #{task.nextTime}, #{task.status}, #{task.createTime})
        </foreach>
    </insert>

    <update id="updateTaskTypeState">
        UPDATE platform_api_task SET status = #{status} WHERE id IN
        <foreach item="task" collection="jobTaskDTOList" separator="," open="(" close=")" index="">
            #{task.id}
        </foreach>
    </update>
</mapper>
