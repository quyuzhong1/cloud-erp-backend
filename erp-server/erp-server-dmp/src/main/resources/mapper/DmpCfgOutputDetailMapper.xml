<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.dmp.mapper.DmpCfgOutputDetailMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.dmp.entity.DmpCfgOutputDetailEntity">
       <result column="id" property="id" />
       <result column="create_time" property="createTime" />
       <result column="update_time" property="updateTime" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_user_id" property="createUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_user_id" property="updateUserId" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="main_id" property="mainId" />
       <result column="next_level_id" property="nextLevelId" />
       <result column="last_time" property="lastTime" />
       <result column="next_time" property="nextTime" />
       <result column="interval_time" property="intervalTime" />
       <result column="override_time" property="overrideTime" />
       <result column="max_retry_count" property="maxRetryCount" />
       <result column="exec_timeout" property="execTimeout" />
       <result column="disabled" property="disabled" />
       <result column="extend_json" property="extendJson" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        dcod.id,
        dcod.create_time,
        dcod.update_time,
        dcod.create_user_name,
        dcod.create_user_id,
        dcod.update_user_name,
        dcod.version,
        dcod.is_deleted,
        dcod.main_id, dcod.next_level_id, dcod.last_time, dcod.next_time, dcod.interval_time, dcod.override_time, dcod.max_retry_count, dcod.exec_timeout, dcod.disabled, dcod.extend_json
    </sql>

    <!-- 查询输出配置明细信息 -->
    <select id="getDmpCfgOutputDetailByOption" resultType="com.erp.model.dmp.entity.DmpCfgOutputDetailEntity">
        select
            dco.id as main_id,
            dcod.id,
            dcod.create_time,
            dcod.update_time,
            dcod.create_user_name,
            dcod.create_user_id,
            dcod.update_user_name,
            dcod.version,
            dcod.is_deleted,
            dcod.next_level_id, dcod.last_time, dcod.next_time, dcod.interval_time, dcod.override_time, dcod.max_retry_count, dcod.exec_timeout, dcod.disabled, dcod.extend_json
        from dmp_cfg_output dco
        left join dmp_cfg_output_detail dcod on dco.id = dcod.main_id and dcod.is_deleted = false and dcod.next_level_id = #{nextLevelId}
        where dco.is_deleted = false and dco.input_convert_id in (
            select id from dmp_cfg_input_convert where is_deleted = false and main_id = #{inputId})
    </select>


</mapper>
