<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.dmp.mapper.DmpDateDimensionMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.dmp.entity.DmpDateDimensionEntity">
       <result column="id" property="id" />
       <result column="create_time" property="createTime" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="update_time" property="updateTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="date_time" property="dateTime" />
       <result column="date_day" property="dateDay" />
       <result column="date_week" property="dateWeek" />
       <result column="date_month" property="dateMonth" />
       <result column="date_quarter" property="dateQuarter" />
       <result column="date_year" property="dateYear" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_time,
        create_user_id,
        create_user_name,
        update_time,
        update_user_id,
        update_user_name,
        version,
        is_deleted,
        date_time, date_day, date_week, date_month, date_quarter, date_year
    </sql>

    <insert id="batchInsertDateDimensions">
        insert into dmp_date_dimension (id,create_user_id,create_user_name,create_time,update_user_id,update_user_name,update_time,version,is_deleted,date_time, date_day, date_week, date_month, date_quarter, date_year) values
        <foreach collection="list" item="entity" separator=",">
            (
            #{entity.id},#{entity.createUserId},#{entity.createUserName},#{entity.createTime},#{entity.updateUserId},#{entity.updateUserName},#{entity.updateTime},
            0,false,
            #{entity.dateTime},
            to_char( #{entity.dateTime}, 'yyyy-mm-dd'),
            concat(date_trunc('WEEK', #{entity.dateTime}::timestamp)::date, '~', (date_trunc('WEEK', #{entity.dateTime}::timestamp)+ '6 days'::interval)::date),
            to_char( #{entity.dateTime}, 'yyyy-mm'),
            concat(to_char( #{entity.dateTime}, 'yyyy'),'-',EXTRACT(QUARTER FROM #{entity.dateTime})),
            to_char( #{entity.dateTime}, 'yyyy')
            )
        </foreach>
    </insert>

    <delete id="deleteByYear">
        delete from dmp_date_dimension where date_year = #{year}
    </delete>
</mapper>
