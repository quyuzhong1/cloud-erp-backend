<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.dmp.pull.mapper.DmpShopChangeLogMapper">

    <select id="getShopChargeName" resultType="com.erp.model.dmp.entity.DmpShopChangeLogEntity">
        SELECT
            id,
            create_time,
            update_time,
            create_user_name,
            create_user_id,
            update_user_name,
            update_user_id,
            charge_id,
            charge_name,
            shop_id,
            enable_time_begin,
            enable_time_end
        FROM dmp_shop_change_log
        WHERE shop_id = #{shopId}
        AND #{date} >= enable_time_begin
        AND #{date} &lt;= enable_time_end
    </select>
</mapper>
