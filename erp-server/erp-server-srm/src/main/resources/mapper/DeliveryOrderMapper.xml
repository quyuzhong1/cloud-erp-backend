<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.srm.mapper.DeliveryOrderMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.srm.entity.DeliveryOrderEntity">
       <result column="id" property="id" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_time" property="createTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_time" property="updateTime" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="code" property="code" />
       <result column="supplier_id" property="supplierId" />
       <result column="source_id" property="sourceId" />
       <result column="plan_delivery_date" property="planDeliveryDate" />
       <result column="receive_code" property="receiveCode" />
       <result column="source_code" property="sourceCode" />
       <result column="customer_name" property="customerName" />
       <result column="contact_id" property="contactId" />
       <result column="contact_name" property="contactName" />
       <result column="to_warehouse_id" property="toWarehouseId" />
       <result column="to_warehouse_name" property="toWarehouseName" />
       <result column="print_date" property="printDate" />
       <result column="confirm_receive_date" property="confirmReceiveDate" />
       <result column="receive_user_id" property="receiveUserId" />
       <result column="receive_user_name" property="receiveUserName" />
       <result column="receive_phone" property="receivePhone" />
       <result column="receive_address" property="receiveAddress" />
       <result column="source_type" property="sourceType" />
       <result column="receipt_status" property="receiptStatus" />
       <result column="is_print" property="isPrint" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_user_id,
        create_user_name,
        create_time,
        update_user_id,
        update_user_name,
        update_time,
        version,
        is_deleted,
        code, supplier_id, source_id, plan_delivery_date, receive_code, source_code, customer_name, contact_id, contact_name, to_warehouse_id, to_warehouse_name, print_date, confirm_receive_date, receive_user_id, receive_user_name, receive_phone, receive_address, source_type, receipt_status, is_print
    </sql>
    <select id="paging" resultType="com.erp.model.srm.dto.DeliveryOrderDTO$ListDTO">
        select
            do2.id,
            doe.id as detailId,
            do2.code,
            doe.source_detail_id as purchaseDetailId,
            do2.plan_delivery_date as planDeliveryDate,
            do2.receive_code as receiveCode,
            do2.is_print as isPrint,
            do2.receipt_status as receiptStatus,
            do2.source_code as sourceCode,
            doe.sku_id as skuId,
            doe.sku_no as skuNo,
            doe.is_urgent as isUrgent,
            doe.product_name as productName,
            doe.order_qty as orderQty,
            doe.delivery_qty as deliveryQty,
            doe.gift_qty as giftQty,
            doe.receive_qty as receiveQty,
            doe.gift_receive_qty as giftReceiveQty,
            doe.qc_good_qty as qcGoodQty,
            doe.remark,
            do2.customer_name as customerName,
            do2.to_warehouse_name as toWarehouseName,
            do2.print_date as printDate,
            do2.confirm_receive_date as confirmReceiveDate,
            do2.create_user_name as createUserName,
            do2.create_time as createTime,
            do2.supplier_id as supplierId
        from delivery_order do2
        inner join delivery_order_detail doe on do2.id = doe.main_id and doe.is_deleted = false
        where
        ${params.sqlMap.default}
        and do2.is_deleted = false
        <if test="params.supplierIdList != null and params.supplierIdList.size > 0">
        and do2.supplier_id in
        <foreach collection="params.supplierIdList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        </if>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        do2.plan_delivery_date ASC,do2.create_time DESC ,do2.id ASC
    </select>
    <select id="tabList" resultType="com.erp.model.srm.dto.DeliveryOrderDTO$StatusListDTO">
        select tab.receiptStatus,tab.isPrint,sum(qtyDifferences) as qtyDifferences,sum(count)  as count from (
        SELECT
        do2.id,
        do2.receipt_status AS receiptStatus,
        do2.is_print AS isPrint,
        case when ( SELECT COUNT ( 1 ) FROM delivery_order_detail doe WHERE doe.main_id = do2.ID AND doe.delivery_qty != doe.receive_qty ) >0 then 1 else 0 end  AS qtyDifferences,
        COUNT ( 1 )   AS COUNT
        FROM
        delivery_order do2
        WHERE
        do2.is_deleted = FALSE
        <if test="supplierIdList != null and supplierIdList.size > 0">
        AND do2.supplier_id IN
        <foreach collection="supplierIdList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        </if>
        GROUP BY
        do2.receipt_status,
        do2.is_print,
        do2.id
        ) tab group by tab.receiptStatus,tab.isPrint
    </select>
    <select id="getExportList" resultType="com.erp.model.srm.dto.excel.DeliveryOrderExportExcelDTO">
        select
        do2.id,
        do2.code,
        doe.source_detail_id as purchaseDetailId,
        do2.plan_delivery_date as planDeliveryDate,
        do2.receive_code as receiveCode,
        do2.is_print as isPrint,
        do2.receipt_status as receiptStatus,
        do2.source_code as sourceCode,
        doe.sku_id as skuId,
        doe.sku_no as skuNo,
        doe.is_urgent as isUrgent,
        doe.product_name as productName,
        doe.order_qty as orderQty,
        doe.delivery_qty as deliveryQty,
        doe.gift_qty as giftQty,
        doe.receive_qty as receiveQty,
        doe.gift_receive_qty as giftReceiveQty,
        doe.qc_good_qty as qcGoodQty,
        do2.customer_name as customerName,
        do2.to_warehouse_name as toWarehouseName,
        do2.print_date as printDate,
        do2.confirm_receive_date as confirmReceiveDate,
        do2.create_user_name as createUserName,
        do2.create_time as createTime,
        do2.supplier_id as supplierId
        from delivery_order do2
        inner join delivery_order_detail doe on do2.id = doe.main_id and doe.is_deleted = false
        where
        ${params.sqlMap.default}
        and do2.is_deleted = false
        <if test="params.supplierIdList != null and params.supplierIdList.size > 0">
        and do2.supplier_id in
        <foreach collection="params.supplierIdList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        </if>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        do2.plan_delivery_date ASC
    </select>
    <select id="listGenerateReceive"
            resultType="com.erp.model.srm.dto.DeliveryOrderDTO$GenerateReceiveListDTO">
        select
            do2.id,
            doe.id as detailId,
            do2.receipt_status as receiptStatus,
            do2.code,
            do2.supplier_id,
            doe.sku_id,
            doe.sku_no,
            doe.product_name,
            doe.delivery_qty,
            doe.gift_qty
        from delivery_order do2
        inner join delivery_order_detail doe on do2.id = doe.main_id
        where do2.is_deleted = false and doe.is_deleted = false
        and do2.id in
        <foreach collection="ids" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
            order by do2.id
    </select>
    <select id="pagingTotal" resultType="com.erp.model.srm.dto.DeliveryOrderDTO$TotalInfo">
        select
            sum(doe.order_qty) as totalOrderQty,
            sum(doe.delivery_qty) as totalDeliveryQty,
            sum(doe.gift_qty) as totalGiftQty,
            sum(doe.receive_qty) as totalReceiveQty,
            sum(doe.gift_receive_qty) as totalGiftReceiveQty
        from delivery_order do2
        inner join delivery_order_detail doe on do2.id = doe.main_id and doe.is_deleted = false
        where
        ${params.sqlMap.default}
        and do2.is_deleted = false

        <if test="params.supplierIdList != null and params.supplierIdList.size > 0">
        and do2.supplier_id in
        <foreach collection="params.supplierIdList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        </if>
    </select>


</mapper>
