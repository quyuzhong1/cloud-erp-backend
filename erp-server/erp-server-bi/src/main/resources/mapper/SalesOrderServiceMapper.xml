<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.erp.server.bi.mapper.SalesOrderServiceMapper">

    <select id="getMonthSales" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        EXTRACT( MONTH FROM o.${timeFlag}) AS flag,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales
        </if>
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.id
        WHERE EXTRACT (YEAR FROM o.${timeFlag} ) = EXTRACT ( YEAR FROM NOW( ) )
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                AND o.platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.platform_create_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND o.delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>
        GROUP BY EXTRACT ( MONTH FROM o.${timeFlag} )
    </select>

    <select id="getBySku" resultType="com.erp.model.bi.vo.SalesVO">
        SELECT
        SUM ( i.quantity ) AS salesQuantity,
        COUNT(DISTINCT ( o.platform_order_id )) AS orderCount,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.sku_no AS name,
        min(i.item_name) AS productName
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.sku_no
        ORDER BY sales DESC
        nulls last
        LIMIT 100

    </select>

    <!--获取近三十天 销售数据-->
    <select id="getLastDays" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT
        SUM ( i.quantity ) AS salesQuantity,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.sku_no AS flagNo,o.${findTime} AS flagDate
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.sku_no,o.${findTime}


    </select>

    <select id="getBySpu" resultType="com.erp.model.bi.vo.SalesVO">
        SELECT
        min(i.item_name) AS productName
        SUM ( i.quantity ) AS salesQuantity,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.sku_no AS name FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY sku_no
        ORDER BY sales DESC
    </select>

    <select id="getByCountry" resultType="com.erp.model.bi.vo.SalesByCountryVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.sku_no AS sku ,
        o.country_name_cn AS country,
        min(i.item_name) AS productName
        FROM dmp_order_item i
        INNER JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>

        GROUP BY i.sku_no,o.country_name_cn
        ORDER BY sales desc
    </select>

    <select id="getPlatformSales" resultType="java.util.Map">
        SELECT o.source_platform AS platform,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales
        </if>
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o
        ON o.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY source_platform
    </select>

    <select id="getByShop" resultType="com.erp.model.bi.vo.ShopSalesVO">
        SELECT a.shop_no,a.sales,a.salesQuantity,s.name AS shopName,s.platform_name,
        a.orderCount
        FROM (
        SELECT o.shop_no,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        SUM ( i.quantity ) AS salesQuantity,
        COUNT(DISTINCT ( o.platform_order_id )) AS orderCount
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.shop_no
        ) a LEFT JOIN dmp_shop_info s ON A.shop_no = s.platform_shop_no
        ORDER BY sales DESC
    </select>
    <select id="getShopLastDays" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT COALESCE( o.shop_no,'') AS flagNo,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        SUM ( i.quantity ) AS salesQuantity, o.${findTime} AS flagDate
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o
        ON o.order_id = o.ID
        WHERE (o.shop_no!=null or o.shop_no!='')
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                AND o.platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.platform_create_time &lt;= #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND o.delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>

        GROUP BY o.shop_no,o.${findTime}
    </select>

    <select id="byTopShop" resultType="java.util.Map">
        SELECT
        o.shop_no,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        SUM ( i.quantity ) AS salesQuantity
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o
        ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.shop_no
        ORDER BY sales DESC limit 20
    </select>


    <select id="byShopCountry" resultType="com.erp.model.bi.vo.ShopSalesVO">
        SELECT
        o.shop_no,
        COALESCE(( SELECT NAME FROM dmp_shop_info WHERE platform_shop_no = o.shop_no LIMIT 1 ),'') AS shopName,
        o.country_name_cn AS platformName,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales
        </if>
        FROM
        dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        WHERE (o.shop_no!='' OR o.shop_no!=NULL)
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                AND o.platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.platform_create_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND o.delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>
        GROUP BY
        o.shop_no,
        country_name_cn
    </select>

    <select id="getCountryList" resultType="com.erp.model.bi.vo.CountryCountVO">
        SELECT country_name_cn AS name,count(*) AS orderCount FROM dmp_order_info
        WHERE country_name_cn!=null or country_name_cn!=''
        GROUP BY  country_name_cn
    </select>


    <select id="byShopNewAndOld" resultType="com.erp.model.bi.vo.ShopSalesVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        SUM ( i.quantity ) AS salesQuantity,o.shop_no,i.new_sign AS flag
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        WHERE
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                o.platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.platform_create_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND o.delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>

        GROUP BY o.shop_no,i.new_sign
    </select>

    <select id="byCountry" resultType="com.erp.model.bi.vo.SalesCountVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        SUM ( i.quantity ) AS salesQuantity,o.country_name_cn AS name,
        count(distinct(o.platform_order_id)) AS orderCount
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.country_name_cn
        ORDER BY sales DESC
    </select>


    <select id="byShopCategory" resultType="com.erp.model.bi.vo.ShopSalesVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.sku_no,o.shop_no,
        ( SELECT NAME FROM dmp_shop_info WHERE platform_shop_no = o.shop_no LIMIT 1 ) AS shopName
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        WHERE (o.shop_no !=null or o.shop_no!='')
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                AND o.platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.platform_create_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND o.delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>
        GROUP BY i.sku_no,o.shop_no
    </select>

    <select id="byCategory" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
        round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>,
        i.sku_no AS flagNo FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.sku_no
    </select>

    <select id="byPlatform" resultType="com.erp.model.bi.vo.SalesCountVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        o.source_platform AS NAME,SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.source_platform
        ORDER BY sales DESC
    </select>

    <select id="byHomeAndAbroad" resultType="com.erp.model.bi.vo.SalesCountVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        country_name_cn AS name FROM
        dmp_order_item i LEFT JOIN dmp_order_info o
        ON i.order_id = o.ID
        WHERE (country_name_cn!=null or country_name_cn!='')
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                AND platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND platform_create_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>

        GROUP BY country_name_cn
    </select>

    <select id="byPeople" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        COALESCE(o.charge_id,'无') AS flagNo, SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount FROM
        dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.charge_id
    </select>

    <select id="byDept" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        COALESCE(o.dept_id,'无') AS flagNo,
        SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.dept_id
    </select>

    <select id="byNewAndOld" resultType="com.erp.model.bi.vo.SalesCountVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        CASE WHEN i.new_sign = 0 THEN '老品' ELSE'新品' END AS name,
        SUM ( i.quantity ) AS salesQuantity,COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.new_sign
    </select>
    <select id="byPlatformNewAndOld" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.new_sign AS flag,o.source_platform AS name,
        SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.new_sign,o.source_platform
        ORDER BY sales DESC
    </select>

    <select id="byCategoryNewAndOld" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.new_sign AS flag,i.sku_no,SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount FROM
        dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.new_sign,i.sku_no
    </select>

    <select id="bySite" resultType="com.erp.model.bi.vo.ShopSalesVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        COALESCE(o.shop_no,'') AS shopNo ,SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.shop_no
    </select>

    <select id="byOldProductTop" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.sku_no AS flagNo FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        WHERE new_sign = 0
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                AND o.platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.platform_create_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND o.delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>
        GROUP BY i.sku_no
        ORDER BY sales DESC LIMIT 20
    </select>
    <select id="byNewProductTop" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.sku_no AS flagNo FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        WHERE new_sign = 1
        <if test="params.timeType!=null and params.timeType==0">
            <if test="params.startTime !=null">
                AND o.platform_create_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.platform_create_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 发货时间 -->
        <if test="params.timeType!=null and params.timeType==1">
            <if test="params.startTime !=null">
                AND o.delivery_time &gt;= #{params.startTime}
            </if>
            <if test="params.endTime !=null">
                AND o.delivery_time &lt; #{params.endTime}
            </if>
        </if>
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>
        GROUP BY i.sku_no
        ORDER BY sales DESC LIMIT 20
    </select>

    <select id="byShop" resultType="com.erp.model.bi.vo.ShopSalesVO">
        SELECT o.shop_no,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales
        </if>
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
        </where>
        GROUP BY o.shop_no
    </select>

    <select id="byBrand" resultType="com.erp.model.bi.vo.SalesCountVO">
        SELECT
        SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        COALESCE(pi.brand_name,'-') AS NAME
        FROM dmp_order_item i
        LEFT JOIN
        dmp_order_info o ON i.order_id = o.ID
        LEFT JOIN product_detail pd ON pd.sku_no = i.sku_no
        LEFT JOIN product_info pi ON pi.ID = pd.product_id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY pi.brand_name
        ORDER BY sales DESC
    </select>


    <select id="byPeopleRank" resultType="com.erp.model.bi.vo.SalesBaseVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        COALESCE(charge_id,'无') AS flagNo FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND delivery_time &lt; #{params.endTime}
                </if>
            </if>
        </where>
        GROUP BY charge_id ORDER BY sales desc
    </select>

    <select id="byPeopleNewAndOld" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.new_sign AS flag,COALESCE(o.charge_id,'无') AS name,
        SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.new_sign,o.charge_id
        ORDER BY sales DESC

    </select>

    <select id="byDeptNewAndOld" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        i.new_sign AS flag,COALESCE(o.dept_id,'无') AS name,SUM ( i.quantity ) AS salesQuantity,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount FROM
        dmp_order_item i LEFT JOIN dmp_order_info o ON 	i.order_id = o.ID
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY i.new_sign,o.dept_id
    </select>

    <select id="byMarketingCenter" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        EXTRACT(MONTH FROM o.${timeFlag}) flag,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount,
        SUM ( i.quantity ) AS salesQuantity FROM
        dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        WHERE
        EXTRACT ( YEAR FROM o.${timeFlag} ) = EXTRACT ( YEAR FROM NOW( ) )

        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>

        GROUP BY
        EXTRACT ( MONTH FROM o.${timeFlag} )
    </select>

    <select id="byLastYear" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        EXTRACT(MONTH FROM o.${timeFlag}) flag,
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount,
        SUM ( i.quantity ) AS salesQuantity FROM
        dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        WHERE
        EXTRACT ( YEAR FROM o.${timeFlag} ) = EXTRACT ( YEAR FROM NOW()-interval '1 years' )
        <!-- 部门 -->
        <if test="params.department!=null and params.department.size>0">
            AND o.dept_id IN
            <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                #{deptId}
            </foreach>
        </if>
        <!-- 平台 -->
        <if test="params.platform!=null and params.platform.size>0">
            AND o.source_platform IN
            <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                #{platform}
            </foreach>
        </if>
        <!-- 站点 -->
        <if test="params.site!=null and params.site.size>0">
            AND o.site IN
            <foreach collection="params.site" item="site" open="(" separator="," close=")">
                #{site}
            </foreach>
        </if>
        <!-- 店铺 -->
        <if test="params.shopName!=null and params.shopName.size>0">
            AND o.shop_name IN
            <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                #{shopName}
            </foreach>
        </if>
        <!-- sku -->
        <if test="params.sku!=null and params.sku.size>0">
            AND i.sku_no IN
            <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                #{sku}
            </foreach>
        </if>
        <!-- 品类 -->
        <if test="params.category!=null and params.category.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.category IN
            <foreach collection="params.category" item="category" open="(" separator="," close=")">
                #{category}
            </foreach>
            )
        </if>
        <!-- 品牌 -->
        <if test="params.brand!=null and params.brand.size>0">
            AND i.sku_no IN
            ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
            pi.brand_name IN
            <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                #{brand}
            </foreach>
            )
        </if>
        <!-- 用户id -->
        <if test="params.userId!=null and params.userId.size>0">
            AND o.charge_id IN
            <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
        <!-- 权限 -->
        <if test="params.param!=null and params.param!=''">
            ${params.param}
        </if>

        GROUP BY
        EXTRACT ( MONTH FROM o.${timeFlag} )
    </select>

    <select id="byLastMonth" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        COUNT ( DISTINCT ( o.platform_order_id ) ) AS orderCount,SUM ( i.quantity ) AS salesQuantity
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>

            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        LIMIT 1
    </select>

    <select id="byTobToc" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT o.source_platform AS name,
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales
        </if>
        FROM dmp_order_item i
        LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>

            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        GROUP BY o.source_platform
    </select>

    <select id="getByDay" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        to_char(o.${timeFlag},'yyyy-mm-dd') AS name,
        SUM ( i.quantity ) AS salesQuantity
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        group by name order by name asc
    </select>

    <select id="getByMonth" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        to_char(o.${timeFlag},'yyyy-mm') AS name,
        SUM ( i.quantity ) AS salesQuantity
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        group by name order by name asc
    </select>

    <select id="getByQuarter" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        date_trunc('QUARTER',o.${timeFlag}) AS name,SUM ( i.quantity ) AS salesQuantity
        FROM dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        group by name order by name asc

    </select>


    <select id="getByYear" resultType="com.erp.model.bi.vo.SalesFlagVO">
        SELECT
        <if test="settleRate !=null and settleRate!=''">
            round(SUM (i.amount_after * i.${settleRate}),2) AS sales,
        </if>
        <if test="settleRate ==null or settleRate==''">
            round(SUM (i.amount_after * 1),2) AS sales,
        </if>
        to_char(o.${timeFlag},'yyyy') as name,
        SUM ( i.quantity ) AS salesQuantity FROM
        dmp_order_item i LEFT JOIN dmp_order_info o ON i.order_id = o.id
        <where>
            <if test="params.timeType!=null and params.timeType==0">
                <if test="params.startTime !=null">
                    o.platform_create_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.platform_create_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 发货时间 -->
            <if test="params.timeType!=null and params.timeType==1">
                <if test="params.startTime !=null">
                    AND o.delivery_time &gt;= #{params.startTime}
                </if>
                <if test="params.endTime !=null">
                    AND o.delivery_time &lt; #{params.endTime}
                </if>
            </if>
            <!-- 部门 -->
            <if test="params.department!=null and params.department.size>0">
                AND o.dept_id IN
                <foreach collection="params.department" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <!-- 平台 -->
            <if test="params.platform!=null and params.platform.size>0">
                AND o.source_platform IN
                <foreach collection="params.platform" item="platform" open="(" separator="," close=")">
                    #{platform}
                </foreach>
            </if>
            <!-- 站点 -->
            <if test="params.site!=null and params.site.size>0">
                AND o.site IN
                <foreach collection="params.site" item="site" open="(" separator="," close=")">
                    #{site}
                </foreach>
            </if>
            <!-- 店铺 -->
            <if test="params.shopName!=null and params.shopName.size>0">
                AND o.shop_name IN
                <foreach collection="params.shopName" item="shopName" open="(" separator="," close=")">
                    #{shopName}
                </foreach>
            </if>
            <!-- sku -->
            <if test="params.sku!=null and params.sku.size>0">
                AND i.sku_no IN
                <foreach collection="params.sku" item="sku" open="(" separator="," close=")">
                    #{sku}
                </foreach>
            </if>
            <!-- 品类 -->
            <if test="params.category!=null and params.category.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.category IN
                <foreach collection="params.category" item="category" open="(" separator="," close=")">
                    #{category}
                </foreach>
                )
            </if>
            <!-- 品牌 -->
            <if test="params.brand!=null and params.brand.size>0">
                AND i.sku_no IN
                ( SELECT sku_no FROM product_detail pd INNER JOIN product_info pi ON pi.id=pd.product_id WHERE
                pi.brand_name IN
                <foreach collection="params.brand" item="brand" open="(" separator="," close=")">
                    #{brand}
                </foreach>
                )
            </if>
            <!-- 用户id -->
            <if test="params.userId!=null and params.userId.size>0">
                AND o.charge_id IN
                <foreach collection="params.userId" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <!-- 权限 -->
            <if test="params.param!=null and params.param!=''">
                ${params.param}
            </if>
        </where>
        group by name order by name asc

    </select>

</mapper>