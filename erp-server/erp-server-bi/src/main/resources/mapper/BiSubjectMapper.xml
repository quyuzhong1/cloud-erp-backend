<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.bi.mapper.BiSubjectMapper">


    <select id="getDashboardFrequentlyList" resultType="com.erp.model.bi.dto.DashboardDTO">
        SELECT id,name FROM  bi_subject
        WHERE category_id=(SELECT id FROM bi_dict WHERE  value=#{dashboardFlag}  AND type=#{type}  LIMIT 1)
        AND is_deleted=0 AND is_frequently=1
        <if test="searchKeyword =null adn searchKeyword!=''">
            AND name LIKE  concat('%',#{searchKeyword},'%')
        </if>
        <if test="findIdList!=null and findIdList.size>0">
          AND id  IN
           <foreach collection="findIdList" item="id" open="(" separator="," close=")">
              #{id}
           </foreach>
        </if>
    </select>

    <select id="getMyCreateDashboardList" resultType="com.erp.model.bi.dto.DashboardDTO">
        SELECT id,name FROM  bi_subject
        WHERE category_id=(SELECT id FROM bi_dict WHERE  value=#{dashboardFlag}  AND type=#{type}  LIMIT 1)
        AND create_user_id=#{userId}
        AND is_deleted=0 AND is_frequently=1
        <if test="searchKeyword !=null adn searchKeyword!=''">
            AND name LIKE  concat('%',#{searchKeyword},'%')
        </if>


    </select>
</mapper>
