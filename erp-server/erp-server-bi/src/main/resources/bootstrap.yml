spring:
  application:
    name: erp-bi
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 500MB
  profiles:
    active: dev
server:
  port: 9070

logging:
  config: classpath:logback-spring.xml

