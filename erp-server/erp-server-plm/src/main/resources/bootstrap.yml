spring:
  application:
    name: erp-plm
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 500MB
  profiles:
    active: dev
server:
  port: 9050

logging:
  config: classpath:logback-spring.xml