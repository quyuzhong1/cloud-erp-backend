<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.plm.mapper.ProductLogisticsMapper">

    <sql id="Base_Column_List">
        pl.product_property,
        pl.product_property_id,declare_chinese_name,declare_english_name,declare_model,
        declare_price,customs_code,declare_unit,declare_element,
        english_material,english_usage,pl.create_time,
        pl.input_voltage,pl.output_voltage,pl.voltage_unit,
        pl.input_electric,pl.output_electric,pl.electric_unit,
        pl.input_power,pl.output_power,pl.power_unit,
        pl.input_battery_capacity,pl.output_battery_capacity,pl.battery_capacity_unit,
        pl.create_user_id,pl.update_time,pl.update_user_id
    </sql>

    <select id="list" resultType="com.erp.model.plm.dto.ProductLogisticsShowDTO">
        SELECT
        pl.id,
        pd.sku_no,
        pd.id AS sku_id,
        pd.images_url,
        <include refid="Base_Column_List"/>
        FROM product_detail pd
        LEFT JOIN product_logistics pl ON pl.sku_id = pd."id" AND pl.is_deleted = FALSE
        WHERE pd.product_id = #{productId} AND pd.is_deleted = FALSE
        ORDER BY pd.id desc
    </select>

    <select id="listBySkuId" resultType="com.erp.model.plm.dto.ProductLogisticsShowDTO">
        SELECT
        pl.id,
        pd.sku_no,
        pd.id AS sku_id,
        pd.images_url,
        <include refid="Base_Column_List"/>
        FROM product_detail pd
        LEFT JOIN product_logistics pl ON pl.sku_id = pd."id" AND pl.is_deleted = FALSE
        WHERE pd.id = #{skuId} AND pd.is_deleted = FALSE
        ORDER BY pd.id desc
    </select>

    <select id="selectSku" resultType="com.erp.model.plm.dto.LogisticsProductDTO$SelectDTO">
        select
           pd.id as skuId,
           pd.sku_no as skuNo
        from  product_detail pd
        inner join product_logistics pl on pd.id = pl.sku_id and pl.is_deleted = false
        <where>
            pd.is_deleted = false and pd.status = 2 and pl.approve_status = 'approve'
        </where>
    </select>


    <select id="getEntityById" resultType="com.erp.model.plm.entity.ProductLogisticsEntity">
        SELECT
            pl.id,
            pd.sku_no,
            pl.sku_id,
            pl.product_property,
            pl.product_property_id,
            pl.declare_model,
            pl.declare_chinese_name,
            pl.declare_english_name,
            pl.declare_price,
            pl.customs_code,
            pl.declare_unit,
            pl.declare_element,
            pl.english_material,
            pl.english_usage,
            pl.declare_currency,
            pl.declare_currency_symbol,
            pl.dest_declare_price,
            pl.dest_currency,
            pl.dest_currency_symbol,
            pl.exemption,
            pl.source_cargo,
            pl.source_country,
            pl.combination_declare_type,
            pl.input_voltage,
            pl.output_voltage,
            pl.voltage_unit,
            pl.input_electric,
            pl.output_electric,
            pl.electric_unit,
            pl.input_power,
            pl.output_power,
            pl.power_unit,
            pl.input_battery_capacity,
            pl.output_battery_capacity,
            pl.battery_capacity_unit,
            pl.approve_time,
            pl.approve_user_name,
            pl.approve_user_id,
            pl.approve_status,
            pl.first_qty,
            pl.second_qty
        FROM product_detail pd
        INNER JOIN product_logistics pl ON pl.sku_id = pd."id" AND pl.is_deleted = FALSE
        WHERE pl.id = #{id} AND pd.is_deleted = FALSE
        ORDER BY pd.id desc
    </select>

</mapper>
