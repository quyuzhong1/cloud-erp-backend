<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.plm.mapper.ProductFieldMapper">

    <select id="sysPaging" resultType="com.erp.model.plm.dto.SysProductFieldPagingDTO">
        SELECT id,name,scope,create_user_id,create_time,
        create_user_name,if_required,type,(state=#{flagState}) AS state,content,update_time
        FROM product_field
        WHERE is_sys=1
        <if test="params.searchKeyword !=null and params.searchKeyword!=''">
            AND name LIKE concat('%',#{params.searchKeyword},'%')
        </if>

        ORDER BY create_time DESC
    </select>

    <select id="paging" resultType="com.erp.model.plm.dto.SysProductFieldPagingDTO">
        SELECT id,name,scope,create_user_id,create_time,
        create_user_name,if_required,type,(state=#{flagState}) AS state,content,update_time
        FROM product_field
        WHERE product_id=#{params.flagId} AND is_sys=0
        <if test="params.searchKeyword !=null and params.searchKeyword!=''">
            AND name LIKE concat('%',#{params.searchKeyword},'%')
        </if>
        ORDER BY create_time DESC
    </select>
</mapper>

