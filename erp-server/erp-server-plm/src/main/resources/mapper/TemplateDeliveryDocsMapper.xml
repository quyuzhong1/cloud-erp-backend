<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.plm.mapper.TemplateDeliveryDocsMapper">


    <select id="paging" resultType="com.erp.model.plm.entity.TemplateDeliveryDocsEntity">
        select
           tdd.id,
           tdd.template_id,
           tdd.docs_name,
           tdd.create_time,
           tdd.update_time,
           tdd.task_id,
           pte.status
        from template_delivery_docs tdd
        left join project_template_entry pte on tdd.template_id = pte.id
        <where>
            <if test="params.docsname != '' and params.docsname != null">
               and tdd.docs_name like concat('%', #{params.docsname} ,'%')
            </if>
        </where>
    </select>

</mapper>
