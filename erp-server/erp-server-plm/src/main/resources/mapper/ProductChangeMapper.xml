<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.plm.mapper.ProductChangeMapper">



    <select id="paging" resultType="com.erp.model.plm.vo.ProductChangePagingVO">
        SELECT
        id,
        type,source_id,state,
        create_time,
        approval_finish_time,
        remark
        FROM product_change
        WHERE
        ${params.sqlMap.default} AND
            is_deleted = FALSE
            <if test="searchList!=null and searchList.size>0">
                AND source_id  IN
               <foreach collection="searchList" item="sourceId" open="(" separator="," close=")">
                   #{sourceId}
               </foreach>
            </if>
        ORDER BY create_time DESC
    </select>


    <select id="getChangeSearchCondition" resultType="java.lang.String">
        SELECT id FROM product_bom_info WHERE  serial_number ILIKE concat('%',#{searchKeyword}::text,'%') AND product_bom_info.is_deleted = FALSE
            UNION ALL
        SELECT id FROM product_detail WHERE
         sku_no!='' AND product_detail.is_deleted = FALSE AND  (sku_no ILIKE concat('%',#{searchKeyword}::text,'%') OR  name ILIKE concat('%',#{searchKeyword}::text,'%'))
    </select>
</mapper>
