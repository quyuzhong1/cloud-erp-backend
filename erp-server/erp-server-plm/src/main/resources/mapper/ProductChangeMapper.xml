<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.plm.mapper.ProductChangeMapper">



    <select id="paging" resultType="com.erp.model.plm.vo.ProductChangePagingVO">
        SELECT
        id,
        type,source_id,state,
        create_time,
        approval_finish_time,
        remark
        FROM product_change
        <where>
            <if test="searchList!=null and searchList.size>0">
                source_id  IN
               <foreach collection="searchList" item="sourceId" open="(" separator="," close=")">
                   #{sourceId}
               </foreach>
            </if>
            <if test="changeIdList !=null and changeIdList.size>0">
               AND id IN
               <foreach collection="changeIdList" item="id" open="(" separator="," close=")">
                #{id}
               </foreach>
            </if>
        </where>
        ORDER BY create_time DESC
    </select>


    <select id="getChangeSearchCondition" resultType="java.lang.String">
        SELECT id FROM product_bom_info WHERE  serial_number LIKE   concat('%',#{searchKeyword},'%')
            UNION ALL
        SELECT id FROM product_detail WHERE
         sku_no!='' AND  (sku_no LIKE concat('%',#{searchKeyword},'%') OR  name LIKE   concat('%',#{searchKeyword},'%'))
    </select>
</mapper>
