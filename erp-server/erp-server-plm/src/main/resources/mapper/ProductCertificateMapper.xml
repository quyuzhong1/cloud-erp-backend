<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.plm.mapper.ProductCertificateMapper">

    <sql id="Base_Column_List">
        pc.sku_id,
        pc.certificate_valid_time,pd.create_time,pd.create_user_id,
        pd.update_time,pd.update_user_id
    </sql>

    <!-- 分页查询 -->
    <select id="paging" resultType="com.erp.model.plm.dto.ProductCertificateDTO$ListDTO">
        select
            pc.id,
            pc.sku_id as skuId,
            pd.sku_no as skuNo,
            pd.name as skuName,
            pc.certificate_valid_time as certificateValidTime,
            pc.type,
            pc.dict_project as dictProject,
            att.attach_name as attachName,
            att.attach_url as attachUrl,
            pc.remark,
            pc.update_user_name as updateUserName,
            pc.update_time as updateTime
        from product_certificate pc
        inner join product_detail pd on pc.sku_id = pd.id and pd.is_deleted = false
        inner join (
            select
                   business_id,attach_name,attach_url
            from attachment
            where id in ( select max(id) from attachment where is_deleted = false group by business_id )
        ) att on pc.id = att.business_id
        <where>
            ${params.sqlMap.default}
            and pc.is_deleted = false
            <if test="params.permissionSql != null and params.permissionSql != ''">
                ${params.permissionSql}
            </if>
        </where>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        pc.id desc
    </select>

    <select id="list" resultType="com.erp.model.plm.dto.ProductCertificateShowDTO">
        SELECT
        pc.id,
        pd.sku_no,
        pd.id AS sku_id,
        pd.images_url,
        <include refid="Base_Column_List"/>
        FROM product_detail pd
        INNER JOIN product_certificate pc ON pc.sku_id = pd."id" AND pc.is_deleted = FALSE
        WHERE pd.product_id = #{productId} AND pd.is_deleted = FALSE
        ORDER BY pd.id desc
    </select>

    <select id="listBySkuId" resultType="com.erp.model.plm.dto.ProductCertificateShowDTO">
        SELECT
        pc.id,
        pd.sku_no,
        pd.id AS sku_id,
        pd.images_url,
        <include refid="Base_Column_List"/>
        FROM product_detail pd
        INNER JOIN product_certificate pc ON pc.sku_id = pd."id" AND pc.is_deleted = FALSE
        WHERE pd.id = #{skuId} AND pd.is_deleted = FALSE
        ORDER BY pd.id desc
    </select>

    <select id="exportExcel" resultType="com.erp.model.plm.dto.ProductCertificateDTO$ListDTO">
        select
        pc.id,
        pc.sku_id as skuId,
        pd.sku_no as skuNo,
        pd.name as skuName,
        pc.certificate_valid_time as certificateValidTime,
        pc.type,
        pc.dict_project as dictProject,
        att.attach_name as attachName,
        att.attach_url as attachUrl,
        pc.remark,
        pc.update_user_name as updateUserName,
        pc.update_time as updateTime
        from product_certificate pc
        inner join product_detail pd on pc.sku_id = pd.id and pd.is_deleted = false
        inner join attachment att on pc.id = att.business_id and att.is_deleted = false
        <where>
            ${params.sqlMap.default}
            and po.is_deleted = false
            <if test="params.permissionSql != null and params.permissionSql != ''">
                ${params.permissionSql}
            </if>
        </where>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        pc.id desc
    </select>

</mapper>
