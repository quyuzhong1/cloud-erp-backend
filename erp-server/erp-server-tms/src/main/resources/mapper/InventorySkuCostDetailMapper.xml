<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.tms.mapper.InventorySkuCostDetailMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.tms.entity.InventorySkuCostDetailEntity">
       <result column="id" property="id" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_time" property="createTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_time" property="updateTime" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="remark" property="remark" />
       <result column="main_id" property="mainId" />
       <result column="sku_no" property="skuNo" />
       <result column="sku_id" property="skuId" />
       <result column="product_name" property="productName" />
       <result column="unit" property="unit" />
       <result column="product_cost" property="productCost" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_user_id,
        create_user_name,
        create_time,
        update_user_id,
        update_user_name,
        update_time,
        version,
        is_deleted,
        remark, main_id, sku_no, sku_id, product_name, unit, product_cost
    </sql>
    <select id="listDetailBySkuIds" resultType="com.erp.model.tms.dto.InventorySkuCostDTO$PagingVO">
        SELECT
        c.id,c.code as code, c.status,c.allocated_month as allocatedMonth, to_char(C.allocated_month, 'YYYY-MM') AS allocatedMonthStr,
        c.currency, c.currency_symbol, c.company_id, c.company_name as companyName,cd.id as detailId,cd.sku_no as skuNo, cd.sku_id,
        cd.product_name, cd.unit, cd.product_cost as productCost,c.create_user_name as createUserName,c.create_time as createTime
        FROM  inventory_sku_cost c
        LEFT JOIN  inventory_sku_cost_detail cd ON cd.main_id=c.id and cd.is_deleted=false
        where c.is_deleted=false
        <if test="skuIds != null and skuIds.size > 0">
            AND cd.sku_id IN
            <foreach collection="skuIds" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        ORDER BY c.create_time DESC
    </select>


</mapper>
