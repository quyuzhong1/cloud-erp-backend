<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.tms.mapper.SmallBagCostAllocationMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.tms.entity.SmallBagCostAllocationEntity">
       <result column="id" property="id" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_time" property="createTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_time" property="updateTime" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="main_id" property="mainId" />
       <result column="sku_id" property="skuId" />
       <result column="sku_no" property="skuNo" />
       <result column="outstock_detail_id" property="outstockDetailId" />
       <result column="delivery_qty" property="deliveryQty" />
       <result column="sku_weight" property="skuWeight" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_user_id,
        create_user_name,
        create_time,
        update_user_id,
        update_user_name,
        update_time,
        version,
        is_deleted,
        main_id, sku_id, sku_no, outstock_detail_id, delivery_qty, sku_weight,
        unit_cost,unit_currency,billing_weight
    </sql>

<select id="paging" resultType="com.erp.model.tms.dto.SmallBagCostAllocationDTO$ListDTO">
        select
            t.id main_id,
            g.id,
            h.id as detailId,
            t.cost_id,
            t.report_date,
            t.account_date,
            t.report_status,
            t.big_table_status,
            g.sku_no,
            g.sku_id,
            cast(g.unit_cost AS NUMERIC(16, 6)) unit_cost,
            g.unit_currency,
            g.delivery_qty,
            cast(g.billing_weight AS NUMERIC(16, 4)) billing_weight,
            cast(g.sku_weight AS NUMERIC(16, 4)) sku_weight,
            t.fee_source,
            h.fee_type,
            cast(h.bill_amount_exchange AS NUMERIC(16, 4)) billAmount,
            cast(h.allocated_amount_exchange AS NUMERIC(16, 2)) allocatedAmount,
            cast(h.product_allocated_amount_exchange AS NUMERIC(16, 6)) productAllocatedAmount,
            h.org_id,
            h.org_name orgName,
            h.fee_allocation_type,
            h.fee_allocation_type feeAllocationTypeName,
            h.weight_allocation_type,
            h.weight_allocation_type weightAllocationTypeName,
            t.create_time createTime,
            t.update_user_name updateUserName,
            t.update_time updateTime,
            h.allocated_currency currency
        from small_bag_cost_allocation_main t
        left join small_bag_cost_allocation g on t.id = g.main_id and g.is_deleted = false
        left join small_bag_cost_allocation_detail h on h.main_id = g.id and h.is_deleted = false
        <!-- 按需 JOIN：仅筛选/排序/权限引用对应别名时才关联；物流展示字段由 Java 层补查 -->
        <if test="params.needJoinJ != null and params.needJoinJ">
            left join logistics_bill_cost j on t.cost_id = j.id and j.is_deleted = false
        </if>
        <if test="params.needJoinK != null and params.needJoinK">
            left join logistics_bill k on j.logistics_bill_id = k.id and k.is_deleted = false
        </if>
        <!-- detail 与 bill 为一对多，默认不 JOIN 避免行数膨胀 -->
        <if test="params.needJoinL != null and params.needJoinL">
            left join logistics_bill_detail l on k.id = l.main_id and l.is_deleted = false
        </if>
        <if test="params.needJoinN != null and params.needJoinN">
            left join dict_basic n on h.fee_type = n.code and n.type = 'dictCostCategory'
        </if>
        <where>
            ${params.sqlMap.default}
            and t.is_deleted = false
            <if test="params.permissionSql!=null and params.permissionSql!=''">
                ${params.permissionSql}
            </if>
        </where>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        t.id desc,g.id,
        case h.fee_type
            when 'shippingCost' then 1
            when 'declareCost' then 2
            when 'otherTaxFee' then 3
            when 'otherCost' then 4
            else 99
        end asc
    </select>
    
<select id="tabList" resultType="com.erp.model.tms.dto.SmallBagCostAllocationDTO$TabListDTO">
        select
            t.report_status tabFlag,count(*) count
        from small_bag_cost_allocation_main t
        <where>
            t.is_deleted = false
            <if test="params.permissionSql!=null and params.permissionSql!=''">
                ${params.permissionSql}
            </if>
        </where>
        group by t.report_status
    </select>
    <select id="listByReportPeriodStr" resultType="com.erp.model.tms.entity.SmallBagCostAllocationEntity">
        select
            sbca.id,sbca.main_id
            from small_bag_cost_allocation sbca
        left join small_bag_cost_allocation_main sbcam on sbca.main_id = sbcam.id and sbcam.is_deleted = false
        where sbca.is_deleted = false
        and sbcam.report_date = #{reportPeriodStr}
        <if test="reportStatus != null and reportStatus!=''">
            and sbcam.report_status = #{reportStatus}
        </if>
    </select>

    <!-- 小包成本分摊 明细列表 -->
    <select id="listSmallBagCost" resultType="com.erp.model.tms.dto.SmallBagCostAllocationDTO$SmallBagCostDTO">
        select
            g.sku_id as skuId,
            g.outstock_detail_id as soOutstockDetailId,
            h.fee_type as feeType,
            h.fee_allocation_type as feeAllocationType,
            h.allocated_currency as allocatedCurrency,
            h.allocated_amount_exchange as allocatedAmountExchange,
            h.allocated_amount as allocatedAmount
        from small_bag_cost_allocation_main t
        left join small_bag_cost_allocation g on t.id = g.main_id and g.is_deleted = false
        left join small_bag_cost_allocation_detail h on h.main_id = g.id and h.is_deleted = false
        where t.is_deleted = false and t.report_status = 'confirmed'
        <if test="param.skuIdList != null and param.skuIdList.size() > 0">
            and g.sku_id in
            <foreach collection="param.skuIdList" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="param.soOutstockDetailIdList != null and param.soOutstockDetailIdList.size() > 0">
            and g.outstock_detail_id in
            <foreach collection="param.soOutstockDetailIdList" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
    </select>
</mapper>
