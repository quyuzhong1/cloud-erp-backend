<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.sys.mapper.SysPostMapper">

    <select id="paging" resultType="com.erp.model.sys.entity.SysPostEntity">
        SELECT id,post_name,post_remark
        FROM sys_post sp
        <where>
            <if test="params.searchKeyword !=null and params.searchKeyword!='' ">
                AND sp.post_name LIKE concat('%',#{params.searchKeyword},'%')
            </if>
        </where>

        <if test="params.startTime!=null">
            AND sp.create_time &gt;=#{params.startTime}
        </if>
        <if test="params.endTime!=null">
            AND sp.update_time &lt;=#{params.endTime}
        </if>

        ORDER BY sp.update_time DESC

    </select>
</mapper>