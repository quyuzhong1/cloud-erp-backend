<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.sys.mapper.SysRoleUserMapper">

    <select id="findRoleUser" resultType="com.erp.model.sys.dto.UserDTO">
        SELECT sru.id AS flagId,sui.uid AS userId,sui.user_name,sui.real_name,sui.mobile
        ,sui.user_state
        FROM sys_role_user sru JOIN sys_user_info sui ON sru.user_id=sui.uid
        WHERE sru.role_id=#{params.flagId}
        AND is_deleted = false
        <if test="params.searchKeyword !=null and params.searchKeyword!='' ">
            sui.user_name ILIKE concat('%',#{params.searchKeyword}::text,'%') OR
            sui.real_name ILIKE concat('%',#{params.searchKeyword}::text,'%')
        </if>

    </select>
    <select id="listRoleByUserIds" resultType="com.erp.model.sys.dto.SysUserDTO$RoleDTO">
        select sru.user_id, sru.role_id,sr.role_name
        FROM sys_role_user sru
        INNER JOIN sys_role sr sru.role_id = sr.id
        where 1 = 1
        <if test="userIds !=null and userIds.size()>0">
            AND sru.user_id in
            <foreach collection="userIds" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
        </if>
    </select>
</mapper>