<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.sys.mapper.MaskCfgSyncMapper">

    <!--
      cfg_mask_field 整表 hash
      只覆盖影响脱敏行为的列：class_path / field_name / strategy / custom_regex /
      custom_replace / permission_code / hide_when_masked / value protect columns / disabled / sort
      remark 等无关字段改动不会触发 Redis 缓存刷新
      空表时 string_agg 返回 null，外面套 coalesce 兜底为空字符串
    -->
    <select id="scanCfgMaskFieldHash" resultType="java.lang.String">
        SELECT coalesce(md5(string_agg(
                   id::text
                   || '|' || coalesce(class_path, '')
                   || '|' || coalesce(field_name, '')
                   || '|' || coalesce(strategy, '')
                   || '|' || coalesce(custom_regex, '')
                   || '|' || coalesce(custom_replace, '')
                   || '|' || coalesce(permission_code, '')
                   || '|' || coalesce(hide_when_masked::text, '')
                   || '|' || coalesce(value_protect_enabled::text, '')
                   || '|' || coalesce(protect_table_name, '')
                   || '|' || coalesce(protect_record_id_column, '')
                   || '|' || coalesce(protect_value_column, '')
                   || '|' || coalesce(protect_deleted_column, '')
                   || '|' || coalesce(protect_param_bindings, '')
                   || '|' || coalesce(protect_mode, '')
                   || '|' || coalesce(disabled::text, '')
                   || '|' || coalesce(sort::text, ''),
               ',' ORDER BY id::text)), '') AS fp
        FROM cfg_mask_field
        WHERE is_deleted = false
    </select>

    <!--
      cfg_mask_word 整表 hash
      只覆盖影响脱敏行为的列：word_type / word / disabled / sort
      category / remark 等业务标记不参与
    -->
    <select id="scanCfgMaskWordHash" resultType="java.lang.String">
        SELECT coalesce(md5(string_agg(
                   id::text
                   || '|' || coalesce(word_type::text, '')
                   || '|' || coalesce(word, '')
                   || '|' || coalesce(disabled::text, '')
                   || '|' || coalesce(sort::text, ''),
               ',' ORDER BY id::text)), '') AS fp
        FROM cfg_mask_word
        WHERE is_deleted = false
    </select>

</mapper>
