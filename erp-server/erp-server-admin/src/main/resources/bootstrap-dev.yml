spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR}
        namespace: ${NACOS_NAMESPACE:dev-k8s}
      config:
        server-addr: ${NACOS_SERVER_ADDR}
        namespace: dev
        file-extension: yml
        shared-configs: common.yml,db-sys.yml,druid-common.yml
sfs:
  nacos:
    application: seata-server
    server-addr: 172.16.100.12:8848
    namespace: seata-server
    group: SEATA_GROUP





