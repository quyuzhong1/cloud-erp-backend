<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.mrp.mapper.ReplenishmentRefLabelMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.mrp.entity.ReplenishmentRefLabelEntity">
       <result column="id" property="id" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_time" property="createTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_time" property="updateTime" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="ref_id" property="refId" />
       <result column="label_id" property="labelId" />
       <result column="type" property="type" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_user_id,
        create_user_name,
        create_time,
        update_user_id,
        update_user_name,
        update_time,
        version,
        is_deleted,
        ref_id, label_id, type
    </sql>

    <!-- 根据关联id查询标签-->
    <select id="listLabelInfoByRefId" resultType="com.erp.model.mrp.dto.LabelInfoDTO$ViewDTO">
        select
            li.id,
            li.name,
            li.color,
            li.disabled
        from  replenishment_ref_label rrl
        left join label_info li on rrl.label_id = li.id and li.is_deleted =false
        <where>
            rrl.is_deleted = false and rrl.ref_id = #{refId}
        </where>
        order by rrl.id desc
    </select>


</mapper>
