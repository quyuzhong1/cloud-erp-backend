<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.mrp.mapper.CalcSalesInfoDimMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.mrp.entity.CalcSalesInfoDimEntity">
        <result column="id" property="id"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_user_name" property="createUserName"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="update_user_name" property="updateUserName"/>
        <result column="update_time" property="updateTime"/>
        <result column="version" property="version"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="sku_id" property="skuId"/>
        <result column="sku_no" property="skuNo"/>
        <result column="country" property="country"/>
        <result column="shop_id" property="shopId"/>
        <result column="platform" property="platform"/>
        <result column="sales_qty_json" property="salesQtyJson"/>
        <result column="avg_sales_qty_json" property="avgSalesQtyJson"/>
        <result column="month_sales_estimate_qty_json" property="monthSalesEstimateQtyJson"/>
        <result column="month_real_sales_qty_json" property="monthRealSalesQtyJson"/>
        <result column="cfg_rule_calc_id" property="cfgRuleCalcId"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_user_id,
        create_user_name,
        create_time,
        update_user_id,
        update_user_name,
        update_time,
        version,
        is_deleted,
        sku_id, sku_no, country, shop_id, platform, sales_qty_json, avg_sales_qty_json, month_sales_estimate_qty_json, month_real_sales_qty_json, cfg_rule_calc_id, remark
    </sql>
    <select id="paging" resultType="com.erp.model.mrp.dto.CalcSalesInfoDimDTO$PagingView">
        select csic.id,
               csic.update_time,
               csic.sku_id,
               csic.sku_no,
               csic.country,
               csic.shop_id,
               csic.platform,
               csic.sales_qty_json,
               csic.avg_sales_qty_json,
               csic.month_sales_estimate_qty_json,
               csic.month_real_sales_qty_json,
               csic.cfg_rule_calc_id
        from calc_sales_info_dim csic
        where ${params.sqlMap.default}
          and csic.is_deleted = FALSE
        and csic.cfg_rule_calc_id = #{params.cfgRuleCalcId}
        order by csic.update_time desc

    </select>
    <select id="view" resultType="com.erp.model.mrp.dto.CalcSalesInfoDimDTO$ViewDTO">
        select id,
               update_time,
               sku_id,
               sku_no,
               country,
               shop_id,
               platform,
               remark,
               cfg_rule_calc_id
        from calc_sales_info_dim
        where id = #{id}
    </select>
    <select id="exportData" resultType="com.erp.model.mrp.dto.CalcSalesInfoDimDTO$ExportDTO">
        select csic.id,
               csic.sku_id,
               csic.sku_no,
               csic.country,
               csic.shop_id,
               csic.platform,
               csic.sales_qty_json,
               csic.avg_sales_qty_json,
               csic.cfg_rule_calc_id,
               crc.start_calc_date,
               crc.end_calc_date
        from calc_sales_info_dim csic
                 inner join cfg_rule_calc crc on csic.cfg_rule_calc_id = crc.id and crc.is_deleted = false
        where ${params.sqlMap.default}
          and csic.is_deleted = FALSE
    </select>
</mapper>
