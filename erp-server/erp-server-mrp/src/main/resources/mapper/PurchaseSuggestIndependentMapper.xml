<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.mrp.mapper.PurchaseSuggestIndependentMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.mrp.entity.PurchaseSuggestEntity">
       <result column="id" property="id" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_time" property="createTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_time" property="updateTime" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="code" property="code" />
       <result column="data_type" property="dataType" />
       <result column="suggest_purchase_qty" property="suggestPurchaseQty" />
       <result column="suggest_purchase_date" property="suggestPurchaseDate" />
       <result column="logistics_method" property="logisticsMethod" />
       <result column="logistics_days" property="logisticsDays" />
       <result column="estimate_instock_date" property="estimateInstockDate" />
       <result column="estimate_sales_date" property="estimateSalesDate" />
       <result column="purchase_cost" property="purchaseCost" />
       <result column="source_type" property="sourceType" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        psm.id,
        psm.create_user_id as createUserId,
        psm.create_user_name as createUserName,
        psm.create_time as createTime,
        psm.update_user_id as updateUserId,
        psm.update_user_name as updateUserName,
        psm.update_time as updateTime,
        psm.code,
        psm.data_type as dataType,
        psm.suggest_purchase_qty as suggestPurchaseQty,
        pss.suggest_purchase_qty as sysSuggestPurchaseQty,
        psm.suggest_purchase_date as suggestPurchaseDate,
        pss.suggest_purchase_date as sysSuggestPurchaseDate,
        pss.suggest_purchase_qty as sysSuggestPurchaseDate,
        psm.logistics_method as logisticsMethod,
        pss.logistics_method as sysLogisticsMethod,
        psm.logistics_days as logisticsDays,
        pss.logistics_days as sysLogisticsDays,
        psm.estimate_instock_date as estimateInstockDate,
        pss.estimate_instock_date as sysEstimateInstockDate,
        psm.estimate_sales_date as estimateSalesDate,
        pss.estimate_sales_date as sysEstimateSalesDate,
        psm.purchase_cost as purchaseCost,
        pss.purchase_cost as sysPurchaseCost,
        psm.invalid_status as invalidStatus,
        psm.invalid_remark as invalidRemark,
        psm.invalid_time as invalidTime,
        psm.invalid_user_id as invalidUserId,
        psm.invalid_user_name as invalidUserName,
        psm.source_id_json as sourceIdJson,
        psm.source_type as sourceType,
        psm.status,
        psm.currency,
        psm.platform_type as platformType,
        psm.platform,
        psm.shop_id as shopId,
        psm.sku_id as skuId,
        psm.plan_purchase_qty as planPurchaseQty,
        psm.purchase_stock_up_qty as purchaseStockUpQty,
        psm.remark
    </sql>

    <!-- 采购建议导出分页 -->
    <select id="pagingExportPurchaseSuggestion"
            resultType="com.erp.model.mrp.dto.ReplenishmentSuggestionDTO$PurchaseSuggestionDTO">
        select
            <include refid="Base_Column_List"/>
            ,pd.sku_no,pd.name as productName,pd.images_url as imagesUrl
        from purchase_suggest_merge psm
        left join purchase_suggest_sys pss on psm.id = pss.source_id and pss.is_deleted = false and psm.data_type = 'auto'
        LEFT JOIN product_detail pd ON psm.sku_id = pd.id AND pd.is_deleted = false
        <where>
            ${params.sqlMap.default}
            and psm.is_deleted = false
        </where>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        psm.id asc
    </select>

    <!-- 列表查询 -->
    <select id="list" resultType="com.erp.model.mrp.dto.PurchaseSuggestIndependentDTO$ListDTO">
        select
            <include refid="Base_Column_List"/>
            ,pd.sku_no,pd.name as productName,pd.images_url as imagesUrl
        from purchase_suggest_merge psm
        LEFT JOIN purchase_suggest_sys pss ON psm.id = pss.source_id AND pss.is_deleted = false AND psm.data_type = 'auto'
        LEFT JOIN product_detail pd ON psm.sku_id = pd.id AND pd.is_deleted = false
        <where>
            psm.is_deleted = false and psm.is_merge = false and psm.status = 'draft' AND psm.source_id_json  @> ('[' || #{params.sourceId} || ']')::jsonb
        </where>
    </select>

    <!-- 分页查询 -->
    <select id="paging" resultType="com.erp.model.mrp.dto.PurchaseSuggestIndependentDTO$ListDTO">
        select
            <include refid="Base_Column_List"/>
            ,pd.sku_no,pd.name as productName,pd.images_url as imagesUrl
        from purchase_suggest_merge psm
        left join purchase_suggest_sys pss on psm.id = pss.source_id and pss.is_deleted = false and psm.data_type = 'auto'
        LEFT JOIN product_detail pd ON psm.sku_id = pd.id AND pd.is_deleted = false
        <where>
            ${params.sqlMap.default}
            and psm.is_deleted = false and psm.is_merge = false
            <if test="params.permissionSql != null and params.permissionSql != ''">
                ${params.permissionSql}
            </if>
        </where>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        psm.id desc
    </select>

</mapper>
