spring:
  cloud:
    nacos:
      discovery:
        server-addr: 10.107.36.15
        namespace: ${NACOS_NAMESPACE:dev}
      config:
        server-addr: 10.107.36.15
        namespace: dev
        shared-configs: common.yml,erp-auth.yml
