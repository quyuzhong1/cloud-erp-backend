spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.100.60:30848
        namespace: ${NACOS_NAMESPACE:dev}
      config:
        server-addr: 172.16.100.60:30848
        namespace: dev
        shared-configs: common.yml,erp-auth.yml
