<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.SampleAdjustmentInfoMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.wms.entity.SampleAdjustmentInfoEntity">
       <result column="id" property="id" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_time" property="createTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_time" property="updateTime" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="code" property="code" />
       <result column="approve_status" property="approveStatus" />
       <result column="approve_time" property="approveTime" />
       <result column="approve_user_id" property="approveUserId" />
       <result column="approve_user_name" property="approveUserName" />
       <result column="invalid_status" property="invalidStatus" />
       <result column="adjustment_user_id" property="adjustmentUserId" />
       <result column="adjustment_user_name" property="adjustmentUserName" />
       <result column="adjustment_dept_id" property="adjustmentDeptId" />
       <result column="adjustment_dept_name" property="adjustmentDeptName" />
       <result column="adjustment_date" property="adjustmentDate" />
       <result column="adjustment_type" property="adjustmentType" />
       <result column="remark" property="remark" />
       <result column="invalid_remark" property="invalidRemark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_user_id,
        create_user_name,
        create_time,
        update_user_id,
        update_user_name,
        update_time,
        version,
        is_deleted,
        code, approve_status, approve_time, approve_user_id, approve_user_name, invalid_status, adjustment_user_id, adjustment_user_name, adjustment_dept_id, adjustment_dept_name, adjustment_date, adjustment_type, remark, invalid_remark
    </sql>

    <sql id="Query_Column_List">
        sai.id AS id,
        sai.create_user_id AS createUserId,
        sai.create_user_name AS createUserName,
        sai.create_time AS createTime,
        sai.update_user_id AS updateUserId,
        sai.update_user_name AS updateUserName,
        sai.update_time AS updateTime,
        sai.version AS version,
        sai.is_deleted AS isDeleted,
        sai.code AS code,
        sai.approve_status AS approveStatus,
        sai.approve_time AS approveTime,
        sai.approve_user_id AS approveUserId,
        sai.approve_user_name AS approveUserName,
        sai.invalid_status AS invalidStatus,
        sai.adjustment_user_id AS adjustmentUserId,
        sai.adjustment_user_name AS adjustmentUserName,
        sai.adjustment_dept_id AS adjustmentDeptId,
        sai.adjustment_dept_name AS adjustmentDeptName,
        sai.adjustment_date AS adjustmentDate,
        sai.adjustment_type AS adjustmentType,
        sai.remark AS remark,
        sai.invalid_remark AS invalidRemark,
        sad.id AS detailId,
        sad.sku_id AS skuId,
        sad.sku_no AS skuNo,
        sad.product_name AS productName,
        sad.ledger_qty AS ledgerQty,
        sad.actual_qty AS actualQty,
        sad.difference_qty AS differenceQty,
        sad.remark AS detailRemark
    </sql>

    <select id="paging" resultType="com.erp.model.wms.dto.SampleAdjustmentInfoDTO$ListDTO">
        SELECT
        <include refid="Query_Column_List"></include>
        FROM
        sample_adjustment_info sai
        LEFT JOIN sample_adjustment_detail sad ON sai.id = sad.main_id AND sad.is_deleted = FALSE
        WHERE
        ${params.sqlMap.default}
        AND sai.is_deleted = FALSE
        <if test="params.permissionSql != null and params.permissionSql != ''">
            ${params.permissionSql}
        </if>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        sai.create_time desc
    </select>

    <select id="listCount" resultType="com.common.business.dto.base.ApproveStatusQtyDTO">
        select approve_status, count(id) from sample_adjustment_info where is_deleted = false group by approve_status
    </select>

    <select id="listExport" resultType="com.erp.model.wms.dto.SampleAdjustmentInfoDTO$ListDTO">
        SELECT
        <include refid="Query_Column_List"></include>
        FROM
        sample_adjustment_info sai
        LEFT JOIN sample_adjustment_detail sad ON sai.id = sad.main_id AND sad.is_deleted = FALSE
        WHERE
        ${params.sqlMap.default}
        AND sai.is_deleted = FALSE
        <if test="params.ids != null and params.ids.size > 0">
            AND sai.id IN
            <foreach collection="params.ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        <if test="params.permissionSql != null and params.permissionSql != ''">
            ${params.permissionSql}
        </if>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        sai.create_time desc
    </select>
    <select id="tabList" resultType="com.erp.model.wms.dto.SampleAdjustmentInfoDTO$TabListDTO">
        SELECT
        'approveIng' AS tabFlag,
        COUNT ( 1 ) AS COUNT
        FROM
        sample_adjustment_info sai
        WHERE
        sai.is_deleted = FALSE
        AND sai.approve_status = 'approveIng'
        <if test="params.ids != null and params.ids.size > 0">
            and sai.id in
            <foreach collection="params.ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        <if test="params != null and params.permissionSql != null and params.permissionSql != ''">
            ${params.permissionSql}
        </if>

        UNION ALL

        SELECT
        'approve' AS tabFlag,
        COUNT ( 1 ) AS COUNT
        FROM
        sample_adjustment_info sai
        WHERE
        sai.is_deleted = FALSE
        AND sai.approve_status = 'approve'
        <if test="params != null and params.permissionSql != null and params.permissionSql != ''">
            ${params.permissionSql}
        </if>

        UNION ALL

        SELECT
        sai.approve_status AS tabFlag,
        COUNT ( * ) AS COUNT
        FROM
        sample_adjustment_info sai
        WHERE
        sai.is_deleted = FALSE
        AND sai.approve_status in ('waitSubmit', 'reject')
        <if test="params != null and params.permissionSql != null and params.permissionSql != ''">
            ${params.permissionSql}
        </if>
        GROUP BY
        sai.approve_status
    </select>

</mapper>
