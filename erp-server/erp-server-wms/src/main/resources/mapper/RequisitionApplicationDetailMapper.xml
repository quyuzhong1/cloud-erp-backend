<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.RequisitionApplicationDetailMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.wms.entity.RequisitionApplicationDetailEntity">
       <result column="id" property="id" />
       <result column="create_time" property="createTime" />
       <result column="update_time" property="updateTime" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_user_id" property="createUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_user_id" property="updateUserId" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="main_id" property="mainId" />
       <result column="sku_id" property="skuId" />
       <result column="sku_no" property="skuNo" />
       <result column="bom_version" property="bomVersion" />
       <result column="requisition_qty" property="requisitionQty" />
       <result column="approve_qty" property="approveQty" />
       <result column="picking_qty" property="pickingQty" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_time,
        update_time,
        create_user_name,
        create_user_id,
        update_user_name,
        update_user_id,
        version,
        is_deleted,
        main_id, sku_id, sku_no, bom_version, requisition_qty, approve_qty, picking_qty
    </sql>

    <!-- 查询要货申请数据 -->
    <select id="listAllVirtualRequisitionApplicationDetail"
            resultType="com.erp.model.wms.dto.ReportOrderDataDTO$ViewDTO">
        select
            rad.from_warehouse_id as warehouseId,
            rad.from_virtual_warehouse_id as virtualWarehouseId,
            rad.sku_id as skuId,
            rad.requisition_qty as qty,
            ra.status,
            ra.invalid_status as invalidStatus,
            ra.id as sourceId,
            ra.code as sourceCode,
            'requisitionApplication' as sourceType,
            rad.id as sourceDetailId,
            ra.create_time as date
        from requisition_application ra
        inner join requisition_application_detail rad on ra.id = rad.main_id and rad.is_deleted = false
        <where>
            ra.is_deleted = false and rad.from_virtual_warehouse_id != '' and rad.from_warehouse_id != ''
        </where>
        order by ra.id desc
    </select>


</mapper>
