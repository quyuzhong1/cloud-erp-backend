<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.SoReturnNoticeDetailMapper">

    <select id="listDetailBySourceIds" resultType="com.erp.model.wms.entity.SoReturnNoticeDetailEntity">
        SELECT
            srnd.id,
            srnd.main_id,
            srnd.sku_id,
            srnd.sku_no,
            srnd.return_qty,
            srnd.return_type_dict,
            srnd.return_reason_dict,
            srnd.remark,
            srnd.source_detail_id,
            srn.approve_status
        FROM so_return_notice_detail srnd
        INNER JOIN so_return_notice srn ON srn.id = srnd.main_id AND srnd.is_deleted = FALSE
        WHERE srn.is_deleted = FALSE
        <if test="ids != null and ids.size != ''">
            and srn.source_id in
            <foreach collection="ids" item="id" open="(" close=")" separator=",">
                #{id}
            </foreach>
        </if>
    </select>

</mapper>
