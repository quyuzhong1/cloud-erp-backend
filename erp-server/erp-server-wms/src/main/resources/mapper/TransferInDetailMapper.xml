<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.TransferInDetailMapper">


    <select id="listSourceDetailIds" resultType="com.erp.model.wms.entity.TransferInDetailEntity">
        select
        tid.id,
        tid.main_id,
        tid.sku_id,
        tid.sku_no,
        tid.qty,
        tid.out_warehouse_location,
        tid.source_detail_id,
        tid.remark
        from transfer_in ti
        inner join transfer_in_detail tid on ti.id = tid.main_id and tid.is_deleted = false
        <where>
            ti.is_deleted = false and ti.invalid_status = false and tid.source_detail_id in
            <foreach collection="sourceDetailIds" item="item" open="(" close=")" separator=",">
                #{item}
            </foreach>
        </where>
    </select>


</mapper>
