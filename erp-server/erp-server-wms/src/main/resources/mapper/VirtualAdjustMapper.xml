<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.VirtualAdjustMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.wms.entity.VirtualAdjustEntity">
       <result column="id" property="id" />
       <result column="create_time" property="createTime" />
       <result column="update_time" property="updateTime" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_user_id" property="createUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_user_id" property="updateUserId" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="code" property="code" />
       <result column="approve_status" property="approveStatus" />
       <result column="bill_date" property="billDate" />
       <result column="approve_time" property="approveTime" />
       <result column="approve_user_name" property="approveUserName" />
       <result column="approve_user_id" property="approveUserId" />
       <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_time,
        update_time,
        create_user_name,
        create_user_id,
        update_user_name,
        update_user_id,
        version,
        is_deleted,
        code, approve_status, bill_date, invalid_status, invalid_remark, approve_time, approve_user_name, approve_user_id, remark
    </sql>

    <select id="paging" resultType="com.erp.model.wms.dto.VirtualAdjustDTO$ListDTO">
        SELECT
            va.id,
            va.code AS code,
            va.approve_status AS approveStatus,
            va.bill_date AS billDate,
            va.approve_user_name AS approveUserName,
            va.approve_time AS approveTime,
            va.remark,
            va.create_time AS createTime,
            va.create_user_name AS createUserName,
            vad.id AS detailId,
            vad.sku_id AS skuId,
            vad.sku_no AS skuNo,
            vad.product_name AS productName,
            vad.virtual_warehouse_id AS virtualWarehouseId,
            vad.virtual_warehouse_name AS virtualWarehouseName,
            vad.inventory_status AS inventoryStatus,
            vad.qty as qty,
            vad.remark as detailRemark
        FROM virtual_adjust va
        LEFT JOIN virtual_adjust_detail vad on vad.main_id = va.id and vad.is_deleted = false
        <where>
            va.is_deleted = false
            <if test="params.sqlMap != null and params.sqlMap.default != null">
                and ${params.sqlMap.default}
            </if>
            <if test="params.permissionSql != null and params.permissionSql != ''">
                ${params.permissionSql}
            </if>
        </where>
        ORDER BY
        <if test="params.sortList != null and params.sortList.size > 0">
            <foreach collection="params.sortList" item="item" separator="," close=",">
                ${item.field} ${item.sort}
            </foreach>
        </if>
        va.create_time desc
    </select>

    <select id="tabList" resultType="com.erp.model.wms.dto.VirtualAdjustDTO$TabListDTO">
        SELECT
        va.approve_status AS tabFlag,
        count(1)  AS count
        FROM  virtual_adjust va
        WHERE va.is_deleted=FALSE
        <if test="params != null and params.permissionSql != null and params.permissionSql != ''">
            ${params.permissionSql}
        </if>
        GROUP BY va.approve_status
    </select>

</mapper>
