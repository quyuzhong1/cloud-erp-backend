<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.WarehouseLocationMapper">

    <select id="paging" resultType="com.erp.model.wms.dto.WarehouseLocationDTO$LocationSelectDTO">
         select distinct code, name from warehouse_location
         <where>
             is_deleted = false
             and type = 'location'
             <if test="params.code != null and params.code != '' ">
                 and code ilike concat('%', #{params.code}::text ,'%')
             </if>
         </where>
         order by code asc NULLS FIRST
    </select>
    <select id="list" resultType="com.erp.model.wms.entity.WarehouseLocationEntity">
        SELECT
            warehouse_id, code, max(name) as name
        FROM
            warehouse_location
        WHERE
            is_deleted = false and type = 'location' and
            warehouse_id IN
            <foreach collection="warehouseIds" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        group by warehouse_id,code
    </select>

    <!-- 根据仓库id和库位集合查询  -->
    <select id="listByWarehouseIdAndCode" resultType="com.erp.model.wms.entity.WarehouseLocationEntity">
        select
           wl.id,
           wl.type,
           wl.code,
           wl.name,
           wl.status,
           wl.warehouse_id,
           wl.parent_id,
           wl.disabled,
           wl.remark,
           wl.occupy_status
        from warehouse_location wl
        where wl.is_deleted = false
        <if test="listParam != null and listParam.size > 0">
            and
            <foreach collection="listParam" item="item" open="(" separator="or" close=")">
               (wl.warehouse_id = #{item.warehouseId} and wl.code = #{item.code})
            </foreach>
        </if>
    </select>
    <select id="pagingByParams" resultType="com.erp.model.wms.dto.WarehouseLocationDTO$PagingViewDTO">
        SELECT
        wl.id as warehouseLocationId,
        wt.id as warehouseId,
        wt.name as warehouseName,
        wt.disabled,
        wt.org_id AS orgId,
        wt.kingdee_warehouse_code AS warehouseCode,
        wt.type_id AS typeId,
        db."name" AS warehouseType,
        wl.code as warehouseLocation,
        wl."name" as warehouseLocationName,
        wa.code as warehouseArea,
        wa."name" as warehouseAreaName
        FROM  "warehouse_location" wl
        LEFT JOIN warehouse wt on wl.warehouse_id = wt.id 
        LEFT JOIN "warehouse_location" wa on wl.parent_id = wa.id
        LEFT JOIN "dict_basic" db on db.id = wt.type_id and db.is_deleted = false and db.type = 'warehouseType'
        where wl.is_deleted = false  and wl."type" = 'location' and wt.disabled = false and wl.disabled = false and wa.disabled = false and wt.approve_status = 'approve'
        <if test="params.typeIds !=null and params.typeIds.size>0">
            AND wt.type_id IN
            <foreach collection="params.typeIds" item="typeId" open="(" separator="," close=")">
                #{typeId}
            </foreach>
        </if>
        <if test="params.orgIds !=null and params.orgIds.size>0">
            AND wt.org_id IN
            <foreach collection="params.orgIds" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="params.warehouseIds !=null and params.warehouseIds.size>0">
            AND wl.warehouse_id IN
            <foreach collection="params.warehouseIds" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="params.warehouseAreas !=null and params.warehouseAreas.size>0">
            AND wa.id IN
            <foreach collection="params.warehouseAreas" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="params.warehouseLocation !=null and params.warehouseLocation != ''">
            AND wl.code ilike concat('%', #{params.warehouseLocation}::text ,'%')
        </if>
        ORDER BY warehouseName,warehouseArea,warehouseLocation desc

    </select>
</mapper>
