<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.VirtualWarehouseChannelMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.wms.entity.VirtualWarehouseChannelEntity">
       <result column="id" property="id" />
       <result column="create_user_id" property="createUserId" />
       <result column="create_user_name" property="createUserName" />
       <result column="create_time" property="createTime" />
       <result column="update_user_id" property="updateUserId" />
       <result column="update_user_name" property="updateUserName" />
       <result column="update_time" property="updateTime" />
       <result column="version" property="version" />
       <result column="is_deleted" property="isDeleted" />
       <result column="disabled" property="disabled" />
       <result column="virtual_warehouse_id" property="virtualWarehouseId" />
       <result column="virtual_warehouse_code" property="virtualWarehouseCode" />
       <result column="virtual_warehouse_name" property="virtualWarehouseName" />
       <result column="dict_platform" property="dictPlatform" />
       <result column="type" property="type" />
       <result column="relation_id" property="relationId" />
       <result column="relation_name" property="relationName" />
       <result column="dict_platform_type" property="dictPlatformType" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        vwc.id,
        vwc.create_user_id,
        vwc.create_user_name,
        vwc.create_time,
        vwc.update_user_id,
        vwc.update_user_name,
        vwc.update_time,
        vwc.version,
        vwc.is_deleted,
        vwc.disabled, vwc.virtual_warehouse_id, vwc.virtual_warehouse_code, vwc.virtual_warehouse_name, vwc.dict_platform, vwc.type, vwc.relation_id, vwc.relation_name, vwc.dict_platform_type
    </sql>
    <select id="getBindedDictPlatform" resultType="java.lang.String">
        SELECT dict_platform FROM "virtual_warehouse_channel"
        where is_deleted=false
          and disabled=false
        group by dict_platform
    </select>
    <select id="getBindedShopByDictPlatform" resultType="java.lang.String">
        SELECT relation_id
        FROM "virtual_warehouse_channel"
        where is_deleted = false
          and disabled = false
          and type = 'shop'
          and dict_platform = #{dictPlatform}
        group by relation_id
    </select>

    <!-- 根据平台参数查询 -->
    <select id="getByPlatform" resultType="com.erp.model.wms.entity.VirtualWarehouseChannelEntity">
        select
               <include refid="Base_Column_List" />
        from  virtual_warehouse_channel vwc
        <where>
            vwc.is_deleted = false and vwc.disabled = false
            and ((vwc.type = 'platform' and  vwc.dict_platform = #{platformDTO.dictPlatform})
            or (vwc.type = 'shop' and vwc.dict_platform = #{platformDTO.dictPlatform} and vwc.relation_id = #{platformDTO.relationId}))
            order by vwc.id limit 1
        </where>
    </select>


</mapper>
