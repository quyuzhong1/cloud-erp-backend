<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.TransferInfoDetailMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.erp.model.wms.entity.TransferInfoDetailEntity">
        <result column="id" property="id" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="create_user_name" property="createUserName" />
        <result column="create_user_id" property="createUserId" />
        <result column="update_user_name" property="updateUserName" />
        <result column="update_user_id" property="updateUserId" />
        <result column="version" property="version" />
        <result column="is_deleted" property="isDeleted" />
        <result column="main_id" property="mainId" />
        <result column="sku_id" property="skuId" />
        <result column="sku_no" property="skuNo" />
        <result column="qty" property="qty" />
        <result column="unit" property="unit" />
        <result column="remark" property="remark" />
    </resultMap>

    <select id="listSourceDetailIds" resultType="com.erp.model.wms.entity.TransferInfoDetailEntity">
        select
            tid.id,
            tid.main_id,
            tid.sku_id,
            tid.sku_no,
            tid.qty,
            tid.unit,
            tid.in_warehouse_location,
            tid.out_warehouse_location,
            tid.remark,
            tid.source_detail_id
        from transfer_info ti
        inner join transfer_info_detail tid on ti.id = tid.main_id and tid.is_deleted = false
        <where>
            ti.is_deleted = false and ti.invalid_status = false and tid.source_detail_id in
            <foreach collection="sourceDetailIds" item="item" open="(" close=")" separator=",">
                #{item}
            </foreach>
        </where>
    </select>

</mapper>
