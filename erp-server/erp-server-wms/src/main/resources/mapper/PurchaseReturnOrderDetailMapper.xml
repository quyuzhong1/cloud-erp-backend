<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.PurchaseReturnOrderDetailMapper">

    <select id="listBySourceDetailIds" resultType="com.erp.model.wms.entity.PurchaseReturnOrderDetailEntity">
        select
            pro.approve_status,
            prod.main_id,
            prod.sku_id,
            prod.sku_no,
            prod.return_qty,
            prod.replenish_qty,
            prod.deduct_amount_qty,
            prod.return_price,
            prod.currency,
            prod.remark,
            prod.source_detail_id,
            prod.purchase_order_detail_id,
            prod.warehouse_location
        from
        po_return pro
        inner join po_return_detail prod on pro.id = prod.main_id and prod.is_deleted = false
        <where>
            pro.is_deleted = false and pro.invalid_status = false
            <if test="sourceDetailIds != null and sourceDetailIds.size > 0">
                and prod.source_detail_id in
                <foreach collection="sourceDetailIds" separator="," item="item" open="(" close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>

    <select id="listReturnOrderDetailByPodIds" resultType="com.erp.model.wms.entity.PurchaseReturnOrderDetailEntity">
        select
        prod.id,
        pro.approve_status,
        prod.main_id,
        prod.sku_id,
        prod.sku_no,
        prod.return_qty,
        prod.replenish_qty,
        prod.deduct_amount_qty,
        prod.return_price,
        prod.currency,
        prod.remark,
        prod.source_detail_id,
        prod.purchase_order_detail_id,
        pro.return_mode
        from
        po_return pro
        inner join po_return_detail prod on pro.id = prod.main_id and prod.is_deleted = false
        <where>
            pro.is_deleted = false and pro.invalid_status = false
            <if test="podIds != null and podIds.size > 0">
                and prod.purchase_order_detail_id in
                <foreach collection="podIds" separator="," item="id" open="(" close=")">
                    #{id}
                </foreach>
            </if>
        </where>
    </select>
</mapper>
