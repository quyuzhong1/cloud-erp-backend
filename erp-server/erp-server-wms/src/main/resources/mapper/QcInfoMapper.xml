<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.server.wms.mapper.QcInfoMapper">


    <select id="getByPurOrderIds" resultType="com.erp.model.wms.dto.QcInfoDTO$QcQtyDTO">
        SELECT
        qi.main_id,qi.qc_type,
        qi.qc_qty,qi.qc_bad_qty,
        qi.total_qty,qi.qc_good_qty,
        qb.purchase_order_id,qi.sku_id
        FROM qc_info qi INNER JOIN  qc_bill qb
        ON  qi.main_id=qb.id
        WHERE qb.purchase_order_id IN
        <foreach collection="purOrderIds" item="purOrderId" open="(" separator="," close=")">
            #{purOrderId}
        </foreach>

    </select>
</mapper>
